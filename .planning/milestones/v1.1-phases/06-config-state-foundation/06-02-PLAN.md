---
phase: 06-config-state-foundation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - agents/ingestion/state.py
  - agents/ingestion/normalizer.py
  - agents/ingestion/main.py
  - agents/ingestion/sources/ws_market_data.py
  - agents/ingestion/sources/candles.py
  - agents/ingestion/sources/funding_rate.py
  - agents/signals/main.py
  - agents/signals/strategies/momentum.py
  - agents/signals/strategies/mean_reversion.py
  - agents/signals/strategies/correlation.py
  - agents/signals/strategies/regime_trend.py
  - agents/signals/strategies/liquidation_cascade.py
  - agents/execution/main.py
  - agents/execution/algo_selector.py
  - agents/execution/stop_loss_manager.py
  - agents/execution/retry_handler.py
  - agents/reconciliation/main.py
  - agents/risk/main.py
  - agents/risk/position_sizer.py
  - agents/ingestion/tests/test_normalizer.py
  - agents/ingestion/tests/test_ws_market_data.py
  - agents/ingestion/tests/test_candles.py
  - agents/ingestion/tests/test_funding_rate.py
  - agents/signals/tests/test_main.py
  - agents/signals/tests/test_momentum.py
  - agents/signals/tests/test_mean_reversion.py
  - agents/signals/tests/test_correlation.py
  - agents/signals/tests/test_regime_trend.py
  - agents/signals/tests/test_liquidation_cascade.py
  - agents/signals/tests/test_orderbook_imbalance.py
  - agents/signals/tests/test_vwap.py
  - agents/signals/tests/test_feature_store.py
  - agents/risk/tests/test_computations.py
  - agents/risk/tests/test_risk_engine.py
  - agents/execution/tests/test_algo_selector.py
autonomous: true
requirements: [MSTA-01, MSTA-02]

must_haves:
  truths:
    - "IngestionState has an instrument_id field set at construction"
    - "Normalizer reads instrument from state.instrument_id, not a constant"
    - "Ingestion main.py creates Dict[str, IngestionState] with one entry per configured instrument"
    - "No file in the codebase imports INSTRUMENT_ID, BASE_CURRENCY, QUOTE_CURRENCY, TICK_SIZE, or MIN_ORDER_SIZE from constants"
    - "Full test suite passes with no import errors"
  artifacts:
    - path: "agents/ingestion/state.py"
      provides: "IngestionState with instrument_id field"
      contains: "instrument_id: str"
    - path: "agents/ingestion/main.py"
      provides: "Per-instrument state dict creation"
      contains: "dict[str, IngestionState]"
    - path: "agents/ingestion/normalizer.py"
      provides: "Instrument from state, not constant"
      contains: "state.instrument_id"
  key_links:
    - from: "agents/ingestion/main.py"
      to: "libs/common/instruments.py"
      via: "get_all_instruments() to create state dict"
      pattern: "get_all_instruments"
    - from: "agents/ingestion/normalizer.py"
      to: "agents/ingestion/state.py"
      via: "state.instrument_id for MarketSnapshot"
      pattern: "state\\.instrument_id"
    - from: "agents/signals/main.py"
      to: "libs/common/instruments.py"
      via: "get_active_instrument_ids() replaces ACTIVE_INSTRUMENT_IDS constant"
      pattern: "get_active_instrument_ids"
---

<objective>
Migrate all callers from hardcoded instrument constants to the config-driven InstrumentConfig registry. Add instrument_id to IngestionState, update normalizer, create per-instrument state dict in main.py, and fix every import across the codebase.

Purpose: Complete the constant removal started in Plan 01. After this plan, zero files import instrument constants from constants.py and the full test suite passes.

Output: Updated IngestionState, normalizer, ingestion main, all signal strategies, execution, risk, reconciliation files, and all corresponding test files.
</objective>

<execution_context>
@/Users/aroman/Work/phantom-perp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aroman/Work/phantom-perp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-config-state-foundation/06-CONTEXT.md
@.planning/phases/06-config-state-foundation/06-RESEARCH.md
@.planning/phases/06-config-state-foundation/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From libs/common/instruments.py (created in Plan 01):
```python
@dataclass(frozen=True, slots=True)
class InstrumentConfig:
    id: str
    base_currency: str
    quote_currency: str
    tick_size: Decimal
    min_order_size: Decimal

    @property
    def ws_product_id(self) -> str:
        return f"{self.id}-INTX"

def load_instruments(yaml_config: dict[str, Any]) -> None: ...
def get_instrument(instrument_id: str) -> InstrumentConfig: ...
def get_all_instruments() -> list[InstrumentConfig]: ...
def get_active_instrument_ids() -> list[str]: ...
```

From libs/common/utils.py (updated in Plan 01 -- no defaults):
```python
def round_to_tick(price: Decimal, tick_size: Decimal) -> Decimal: ...
def round_size(size: Decimal, min_size: Decimal) -> Decimal: ...
```

From libs/coinbase/rest_client.py (updated in Plan 01 -- no defaults):
```python
async def get_orderbook(self, instrument_id: str, ...) -> OrderBookResponse: ...
async def get_candles(self, instrument_id: str, ...) -> list[CandleResponse]: ...
async def get_funding_rate(self, instrument_id: str) -> FundingRateResponse: ...
```

From agents/ingestion/state.py (current):
```python
@dataclass
class IngestionState:
    best_bid: Decimal | None = None
    best_ask: Decimal | None = None
    # ... all fields have defaults
```

From agents/ingestion/normalizer.py (current):
```python
from libs.common.constants import INSTRUMENT_ID
def build_snapshot(state: IngestionState) -> MarketSnapshot | None:
    return MarketSnapshot(instrument=INSTRUMENT_ID, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update IngestionState, normalizer, and ingestion sources</name>
  <files>
    agents/ingestion/state.py,
    agents/ingestion/normalizer.py,
    agents/ingestion/main.py,
    agents/ingestion/sources/ws_market_data.py,
    agents/ingestion/sources/candles.py,
    agents/ingestion/sources/funding_rate.py
  </files>
  <read_first>
    agents/ingestion/state.py
    agents/ingestion/normalizer.py
    agents/ingestion/main.py
    agents/ingestion/sources/ws_market_data.py
    agents/ingestion/sources/candles.py
    agents/ingestion/sources/funding_rate.py
    libs/common/instruments.py
  </read_first>
  <action>
    1. **agents/ingestion/state.py** -- Add `instrument_id: str` as the FIRST field in `IngestionState` (before `best_bid`). This is a required field with no default, so it must be passed at construction: `IngestionState(instrument_id="ETH-PERP")`. All other fields keep their existing defaults.

    2. **agents/ingestion/normalizer.py** -- Remove `from libs.common.constants import INSTRUMENT_ID` (line 14). Change `build_snapshot()` line 65 from `instrument=INSTRUMENT_ID` to `instrument=state.instrument_id`. No other changes needed -- the function signature stays `build_snapshot(state: IngestionState)`.

    3. **agents/ingestion/main.py** -- Add `from libs.common.instruments import get_all_instruments` to imports. Replace `state = IngestionState()` (line 40) with:
       ```python
       instruments = get_all_instruments()
       states: dict[str, IngestionState] = {
           inst.id: IngestionState(instrument_id=inst.id)
           for inst in instruments
       }
       ```
       For now, the rest of main.py continues to use only the first instrument's state for WS, candles, and funding (multi-instrument routing comes in Phases 7-8). Add a comment: `# Phase 6: per-instrument states created; Phase 7/8 will use all of them`. Change `state` references in `run_ws_market_data(ws_client, state, ...)`, `run_all_candle_pollers(rest_client, state)`, and `run_funding_poller(rest_client, state, publisher)` to `states["ETH-PERP"]` (the current default instrument). Update the `on_ws_update` closure to use `states["ETH-PERP"]` instead of `state`.

    4. **agents/ingestion/sources/ws_market_data.py** -- Remove the hardcoded `WS_PRODUCT_ID = "ETH-PERP-INTX"` constant (line 24). Add a parameter `ws_product_id: str` to the `run_ws_market_data()` function signature. In `agents/ingestion/main.py`, pass `ws_product_id=instruments[0].ws_product_id` (or derive from `get_instrument("ETH-PERP").ws_product_id`) when calling `run_ws_market_data()`. Update all internal references to `WS_PRODUCT_ID` within ws_market_data.py to use the `ws_product_id` parameter. Read the full file first to find all references.

    5. **agents/ingestion/sources/candles.py** -- Remove `from libs.common.constants import INSTRUMENT_ID` (line 15). Add `instrument_id: str` parameter to `run_all_candle_pollers()` and pass it through to the REST client call at line 60 where `instrument_id=INSTRUMENT_ID` becomes `instrument_id=instrument_id`. In main.py, pass `instrument_id="ETH-PERP"` when calling `run_all_candle_pollers()`. Read full file first to trace parameter threading.

    6. **agents/ingestion/sources/funding_rate.py** -- Remove `from libs.common.constants import INSTRUMENT_ID` (line 17). Add `instrument_id: str` parameter to `run_funding_poller()`. Update the REST call at line 44 from `instrument_id=INSTRUMENT_ID` to `instrument_id=instrument_id`. Update the publish dict at line 84 from `"instrument": INSTRUMENT_ID` to `"instrument": instrument_id`. In main.py, pass `instrument_id="ETH-PERP"` when calling `run_funding_poller()`.
  </action>
  <verify>
    <automated>python3 -m pytest agents/ingestion/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - agents/ingestion/state.py contains `instrument_id: str` as first field in IngestionState
    - agents/ingestion/normalizer.py does NOT contain `from libs.common.constants`
    - agents/ingestion/normalizer.py contains `instrument=state.instrument_id`
    - agents/ingestion/main.py contains `dict[str, IngestionState]`
    - agents/ingestion/main.py contains `get_all_instruments`
    - agents/ingestion/main.py contains `states["ETH-PERP"]`
    - agents/ingestion/sources/ws_market_data.py does NOT contain `WS_PRODUCT_ID = "ETH-PERP-INTX"`
    - agents/ingestion/sources/candles.py does NOT contain `from libs.common.constants`
    - agents/ingestion/sources/funding_rate.py does NOT contain `from libs.common.constants`
    - `python3 -m pytest agents/ingestion/tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>IngestionState has instrument_id field. Normalizer uses state.instrument_id. main.py creates per-instrument state dict. All ingestion sources accept instrument_id as parameter. Ingestion tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all non-ingestion callers (signals, execution, risk, reconciliation)</name>
  <files>
    agents/signals/main.py,
    agents/signals/strategies/momentum.py,
    agents/signals/strategies/mean_reversion.py,
    agents/signals/strategies/correlation.py,
    agents/signals/strategies/regime_trend.py,
    agents/signals/strategies/liquidation_cascade.py,
    agents/execution/main.py,
    agents/execution/algo_selector.py,
    agents/execution/stop_loss_manager.py,
    agents/execution/retry_handler.py,
    agents/reconciliation/main.py,
    agents/risk/main.py,
    agents/risk/position_sizer.py
  </files>
  <read_first>
    agents/signals/main.py
    agents/signals/strategies/momentum.py
    agents/execution/main.py
    agents/execution/algo_selector.py
    agents/execution/stop_loss_manager.py
    agents/execution/retry_handler.py
    agents/reconciliation/main.py
    agents/risk/main.py
    agents/risk/position_sizer.py
    libs/common/instruments.py
  </read_first>
  <action>
    **Signals agent callers:**

    1. **agents/signals/main.py** (line 29): Replace `from libs.common.constants import ACTIVE_INSTRUMENT_IDS` with `from libs.common.instruments import get_active_instrument_ids`. At line 326, change `instrument_ids = yaml_instruments if yaml_instruments else ACTIVE_INSTRUMENT_IDS` to `instrument_ids = yaml_instruments if yaml_instruments else get_active_instrument_ids()`.

    2. **agents/signals/strategies/momentum.py** (line 25): Remove `from libs.common.constants import INSTRUMENT_ID  # backward compat default`. This import is UNUSED -- the strategy already uses `snapshot.instrument` for signal creation. Just delete the import line.

    3. **agents/signals/strategies/mean_reversion.py** (line 26): Remove `from libs.common.constants import INSTRUMENT_ID`. Unused import -- delete the line.

    4. **agents/signals/strategies/correlation.py** (line 22): Remove `from libs.common.constants import INSTRUMENT_ID`. Unused import -- delete the line.

    5. **agents/signals/strategies/regime_trend.py** (line 27): Remove `from libs.common.constants import INSTRUMENT_ID`. Unused import -- delete the line.

    6. **agents/signals/strategies/liquidation_cascade.py** (line 25): Remove `from libs.common.constants import INSTRUMENT_ID`. Unused import -- delete the line.

    **Execution agent callers:**

    7. **agents/execution/main.py** (line 30): Change `from libs.common.constants import FEE_MAKER, FEE_TAKER, INSTRUMENT_ID` to `from libs.common.constants import FEE_MAKER, FEE_TAKER`. INSTRUMENT_ID is unused in this file.

    8. **agents/execution/algo_selector.py** (line 13): Remove `from libs.common.constants import TICK_SIZE`. Add `from libs.common.instruments import get_instrument`. At line 141, the `_round_to_tick` helper uses `TICK_SIZE` directly. Change it to accept `instrument_id: str` parameter and look up tick_size: `tick_size = get_instrument(instrument_id).tick_size`. Read the full file to find where `_round_to_tick` is called and pass the instrument_id through.

    9. **agents/execution/stop_loss_manager.py** (line 12): Remove `from libs.common.constants import TICK_SIZE`. Add `from libs.common.instruments import get_instrument`. At line 105, the `_round_price` helper uses `TICK_SIZE`. Change it to accept `instrument_id: str` and look up `get_instrument(instrument_id).tick_size`. Read full file to trace call chain and thread instrument_id.

    10. **agents/execution/retry_handler.py** (line 102): Remove the lazy import `from libs.common.constants import TICK_SIZE` inside the function body. Add `from libs.common.instruments import get_instrument` at the module level. At line 109, replace `TICK_SIZE` with a lookup: get the instrument_id from the order/context being retried, then `tick_size = get_instrument(instrument_id).tick_size`. Read full file to understand available context.

    **Risk agent callers:**

    11. **agents/risk/main.py** (lines 24-25): Change the import block to remove `INSTRUMENT_ID` and `MIN_ORDER_SIZE` from the constants import. Add `from libs.common.instruments import get_instrument`. At line 211, replace `MIN_ORDER_SIZE` with a lookup from the trade idea's instrument: `min_size = get_instrument(idea.instrument).min_order_size` (or equivalent -- read context). At line 324, replace `instrument=INSTRUMENT_ID` with `instrument=idea.instrument` (use the instrument from the ranked trade idea).

    12. **agents/risk/position_sizer.py** (line 11): Remove `from libs.common.constants import MIN_ORDER_SIZE`. Add `from libs.common.instruments import get_instrument`. At line 69, replace `MIN_ORDER_SIZE` with a lookup using the instrument_id that should be available from the sizing context. Read full file to determine how instrument_id flows in.

    **Reconciliation agent:**

    13. **agents/reconciliation/main.py** (line 31): Remove `from libs.common.constants import INSTRUMENT_ID`. This import is UNUSED in the file (verified by grep). Just delete the import line.
  </action>
  <verify>
    <automated>python3 -c "import ast, sys; [ast.parse(open(f).read()) for f in ['agents/signals/main.py','agents/signals/strategies/momentum.py','agents/signals/strategies/mean_reversion.py','agents/signals/strategies/correlation.py','agents/signals/strategies/regime_trend.py','agents/signals/strategies/liquidation_cascade.py','agents/execution/main.py','agents/execution/algo_selector.py','agents/execution/stop_loss_manager.py','agents/execution/retry_handler.py','agents/reconciliation/main.py','agents/risk/main.py','agents/risk/position_sizer.py']]; print('all files parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - agents/signals/main.py contains `from libs.common.instruments import get_active_instrument_ids`
    - agents/signals/main.py contains `get_active_instrument_ids()`
    - agents/signals/main.py does NOT contain `ACTIVE_INSTRUMENT_IDS`
    - agents/signals/strategies/momentum.py does NOT contain `from libs.common.constants`
    - agents/signals/strategies/mean_reversion.py does NOT contain `from libs.common.constants`
    - agents/signals/strategies/correlation.py does NOT contain `from libs.common.constants`
    - agents/signals/strategies/regime_trend.py does NOT contain `from libs.common.constants`
    - agents/signals/strategies/liquidation_cascade.py does NOT contain `from libs.common.constants`
    - agents/execution/main.py does NOT contain `INSTRUMENT_ID`
    - agents/execution/algo_selector.py does NOT contain `from libs.common.constants import TICK_SIZE`
    - agents/execution/stop_loss_manager.py does NOT contain `from libs.common.constants import TICK_SIZE`
    - agents/execution/retry_handler.py does NOT contain `from libs.common.constants import TICK_SIZE`
    - agents/risk/main.py does NOT contain `INSTRUMENT_ID` or `MIN_ORDER_SIZE` (except in human-readable strings)
    - agents/risk/position_sizer.py does NOT contain `from libs.common.constants import MIN_ORDER_SIZE`
    - agents/reconciliation/main.py does NOT contain `INSTRUMENT_ID`
    - All 13 files parse without syntax errors
  </acceptance_criteria>
  <done>All non-ingestion callers migrated from constants to config-driven instrument lookups. No file in agents/ imports INSTRUMENT_ID, BASE_CURRENCY, QUOTE_CURRENCY, TICK_SIZE, MIN_ORDER_SIZE, or ACTIVE_INSTRUMENT_IDS from constants.py.</done>
</task>

<task type="auto">
  <name>Task 3: Update all test files and verify full suite green</name>
  <files>
    agents/ingestion/tests/test_normalizer.py,
    agents/ingestion/tests/test_ws_market_data.py,
    agents/ingestion/tests/test_candles.py,
    agents/ingestion/tests/test_funding_rate.py,
    agents/signals/tests/test_main.py,
    agents/signals/tests/test_momentum.py,
    agents/signals/tests/test_mean_reversion.py,
    agents/signals/tests/test_correlation.py,
    agents/signals/tests/test_regime_trend.py,
    agents/signals/tests/test_liquidation_cascade.py,
    agents/signals/tests/test_orderbook_imbalance.py,
    agents/signals/tests/test_vwap.py,
    agents/signals/tests/test_feature_store.py,
    agents/risk/tests/test_computations.py,
    agents/risk/tests/test_risk_engine.py,
    agents/execution/tests/test_algo_selector.py
  </files>
  <read_first>
    agents/ingestion/tests/test_normalizer.py
    agents/signals/tests/test_main.py
    agents/signals/tests/test_momentum.py
    agents/risk/tests/test_computations.py
    agents/risk/tests/test_risk_engine.py
    agents/execution/tests/test_algo_selector.py
  </read_first>
  <action>
    For ALL test files that import `from libs.common.constants import INSTRUMENT_ID`:

    **Pattern:** Replace the import with a string literal `TEST_INSTRUMENT_ID = "ETH-PERP"` at the top of each file. Replace all usages of `INSTRUMENT_ID` in the file with `TEST_INSTRUMENT_ID`. This is explicit and avoids test dependency on the instrument registry.

    For test files that import `MIN_ORDER_SIZE` or `TICK_SIZE`:
    Replace with `Decimal("0.0001")` and `Decimal("0.01")` literals respectively, or define constants at top of test file.

    **Specific files:**

    1. **agents/ingestion/tests/test_normalizer.py**: Replace `from libs.common.constants import INSTRUMENT_ID` with `TEST_INSTRUMENT_ID = "ETH-PERP"`. Update `IngestionState()` construction to `IngestionState(instrument_id=TEST_INSTRUMENT_ID)`. Replace assertion `snapshot.instrument == INSTRUMENT_ID` with `snapshot.instrument == TEST_INSTRUMENT_ID`. Same for `d["instrument"] == INSTRUMENT_ID`.

    2. **agents/ingestion/tests/test_ws_market_data.py**: If it references `WS_PRODUCT_ID`, update to use `"ETH-PERP-INTX"` string literal. Update any `IngestionState()` to include `instrument_id="ETH-PERP"`.

    3. **agents/ingestion/tests/test_candles.py**: Update `IngestionState()` to `IngestionState(instrument_id="ETH-PERP")` if present. Update any `INSTRUMENT_ID` references.

    4. **agents/ingestion/tests/test_funding_rate.py**: Update any `INSTRUMENT_ID` references and `IngestionState()` calls.

    5. **agents/signals/tests/test_main.py** (imports at line 6, uses at lines 17, 36, 48, 78): Replace import with `TEST_INSTRUMENT_ID = "ETH-PERP"`. Update all 4 usage sites.

    6. **agents/signals/tests/test_momentum.py** (lines 9, 27, 151): Replace import, update 2 usage sites.

    7. **agents/signals/tests/test_mean_reversion.py** (lines 8, 24, 112, 174, 656): Replace import, update 4 usage sites.

    8. **agents/signals/tests/test_correlation.py** (lines 8, 26, 456): Replace import, update 2 usage sites.

    9. **agents/signals/tests/test_regime_trend.py** (lines 9, 28, 184): Replace import, update 2 usage sites.

    10. **agents/signals/tests/test_liquidation_cascade.py** (lines 8, 31): Replace import, update 1 usage site.

    11. **agents/signals/tests/test_orderbook_imbalance.py** (lines 8, 30): Replace import, update 1 usage site.

    12. **agents/signals/tests/test_vwap.py** (lines 18, 52): Replace import, update 1 usage site.

    13. **agents/signals/tests/test_feature_store.py** (lines 9, 23, 137): Replace import, update 2 usage sites.

    14. **agents/risk/tests/test_computations.py** (line 8): Replace `from libs.common.constants import FEE_MAKER, FEE_TAKER, MIN_ORDER_SIZE` -- keep FEE_MAKER and FEE_TAKER imports, replace MIN_ORDER_SIZE with `TEST_MIN_ORDER_SIZE = Decimal("0.0001")`. Update line 328.

    15. **agents/risk/tests/test_risk_engine.py** (line 17): Replace `INSTRUMENT_ID` import with `TEST_INSTRUMENT_ID = "ETH-PERP"`. Update lines 83, 125, 555, 567.

    16. **agents/execution/tests/test_algo_selector.py**: If it references TICK_SIZE, update to use `Decimal("0.01")` literal. Update any calls that changed signature in algo_selector.py.

    After updating all files, also ensure instrument registry is loaded in any test that needs it (e.g., tests for risk/main.py or execution modules that now call `get_instrument()`). Add a conftest fixture or inline `load_instruments()` call where needed using the test YAML structure from 06-RESEARCH.md.

    Finally, run the full test suite and fix any remaining failures.
  </action>
  <verify>
    <automated>python3 -m pytest agents/ libs/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - No test file contains `from libs.common.constants import INSTRUMENT_ID`
    - No test file contains `from libs.common.constants import ACTIVE_INSTRUMENT_IDS`
    - agents/ingestion/tests/test_normalizer.py contains `IngestionState(instrument_id=`
    - agents/ingestion/tests/test_normalizer.py contains `TEST_INSTRUMENT_ID = "ETH-PERP"`
    - agents/signals/tests/test_main.py contains `TEST_INSTRUMENT_ID = "ETH-PERP"`
    - agents/risk/tests/test_risk_engine.py contains `TEST_INSTRUMENT_ID = "ETH-PERP"`
    - `python3 -m pytest agents/ libs/ -x -q` exits 0
    - `grep -r "from libs.common.constants import.*INSTRUMENT_ID" agents/ libs/` returns no results
    - `grep -r "from libs.common.constants import.*ACTIVE_INSTRUMENT_IDS" agents/ libs/` returns no results
    - `grep -r "from libs.common.constants import.*BASE_CURRENCY" agents/ libs/` returns no results
    - `grep -r "from libs.common.constants import.*QUOTE_CURRENCY" agents/ libs/` returns no results
  </acceptance_criteria>
  <done>All test files updated to use string literals instead of removed constants. IngestionState construction in tests includes instrument_id. Full test suite (agents/ and libs/) passes green. Zero imports of removed constants remain anywhere in the codebase.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `python3 -m pytest agents/ libs/ -x -q` -- full suite green
2. `grep -r "from libs.common.constants import.*INSTRUMENT_ID\|from libs.common.constants import.*BASE_CURRENCY\|from libs.common.constants import.*QUOTE_CURRENCY\|from libs.common.constants import.*TICK_SIZE\|from libs.common.constants import.*MIN_ORDER_SIZE\|from libs.common.constants import.*ACTIVE_INSTRUMENT_IDS" agents/ libs/ --include="*.py"` -- returns empty (no imports of removed constants)
3. `python3 -c "from libs.common.config import get_settings; get_settings(); from agents.ingestion.state import IngestionState; s = IngestionState(instrument_id='ETH-PERP'); print(s.instrument_id)"` -- prints ETH-PERP
</verification>

<success_criteria>
- IngestionState.instrument_id exists and is required at construction
- Normalizer uses state.instrument_id (not constant)
- main.py creates dict[str, IngestionState] from config
- All ~30 caller files migrated from constants to config/parameters
- All ~16 test files updated with string literals
- Full test suite passes
- Zero references to removed constants in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/06-config-state-foundation/06-02-SUMMARY.md`
</output>
