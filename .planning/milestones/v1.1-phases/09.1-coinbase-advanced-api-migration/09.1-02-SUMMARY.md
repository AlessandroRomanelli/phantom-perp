---
phase: 09.1-coinbase-advanced-api-migration
plan: 02
subsystem: api
tags: [coinbase, rest-api, advanced-trade, pydantic, migration]

# Dependency graph
requires:
  - phase: 09.1-01
    provides: "ES256 JWT auth, CoinbaseAuth.sign(), DEFAULT_REST_BASE_URL, CoinbaseSettings, PortfolioIDSettings"
provides:
  - "All 10 REST endpoints using /api/v3/brokerage/ paths"
  - "Advanced Trade response models with Amount objects and string numeric fields"
  - "Portfolio UUID injection through CoinbaseClientPool"
  - "create_order with order_configuration structure"
  - "cancel_order via POST batch_cancel pattern"
  - "get_funding_rate extraction from product details"
affects: [09.1-03, 09.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Amount model for monetary values {value, currency}"
    - "Response unwrapping (pricebook, summary, success_response)"
    - "Portfolio UUID in path for /intx/ endpoints"

key-files:
  created: []
  modified:
    - libs/coinbase/models.py
    - libs/coinbase/rest_client.py
    - libs/coinbase/client_pool.py

key-decisions:
  - "Kept get_instruments() as alias to get_products() for backward compat in callers"
  - "String fields for all numeric values in responses (matching Advanced Trade JSON shape)"
  - "FundingRateResponse constructed manually from product details (no dedicated endpoint)"

patterns-established:
  - "Amount(value, currency) pattern for Advanced Trade monetary fields"
  - "Response wrapper unwrapping: data.get('key', data) for nested structures"

requirements-completed: [MIG-02, MIG-03]

# Metrics
duration: 4min
completed: 2026-03-23
---

# Phase 09.1 Plan 02: REST Endpoint Migration Summary

**All 10 REST endpoints rewritten to Advanced Trade API paths with Amount-based response models and portfolio UUID injection**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-23T14:13:02Z
- **Completed:** 2026-03-23T14:17:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Rewrote all 10 Pydantic response models to match Advanced Trade JSON shapes (Amount objects, string fields, renamed fields)
- Migrated all REST endpoint methods from /api/v1/ to /api/v3/brokerage/ paths with zero legacy paths remaining
- Added portfolio_uuid support to CoinbaseRESTClient and CoinbaseClientPool for portfolio-scoped endpoints

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite models.py for Advanced Trade response shapes** - `dcc65ce` (feat)
2. **Task 2: Rewrite rest_client.py endpoints and update client_pool.py** - `c2f1ba1` (feat)

## Files Created/Modified
- `libs/coinbase/models.py` - Advanced Trade response models (Amount, ProductResponse, string CandleResponse, Amount-based PositionResponse/PortfolioResponse, renamed FillResponse fields)
- `libs/coinbase/rest_client.py` - All 10 endpoint methods using /api/v3/brokerage/ paths with new request/response patterns
- `libs/coinbase/client_pool.py` - Portfolio UUID injection (portfolio_uuid_a, portfolio_uuid_b passed to clients)

## Decisions Made
- Kept `get_instruments()` as backward-compatible alias for `get_products()` -- callers in ingestion/signals agents can migrate incrementally
- All numeric response fields stored as strings (matching Advanced Trade's JSON format) -- downstream agents handle Decimal conversion
- FundingRateResponse manually constructed from product details nested path (`future_product_details.perpetual_details.funding_rate`) since Advanced Trade has no dedicated funding endpoint

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- REST client fully targets Advanced Trade API -- ready for Plan 03 (caller adaptation in agents)
- Callers still reference old model fields (instrument_id, fee, fill_id) -- Plan 03 will update them
- Product ID discovery (Plan 04) will verify actual perpetual contract IDs

## Self-Check: PASSED

- All 3 modified files verified on disk
- Both task commits (dcc65ce, c2f1ba1) verified in git log

---
*Phase: 09.1-coinbase-advanced-api-migration*
*Completed: 2026-03-23*
