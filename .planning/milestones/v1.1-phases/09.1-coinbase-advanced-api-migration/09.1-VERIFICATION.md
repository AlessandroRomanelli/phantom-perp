---
phase: 09.1-coinbase-advanced-api-migration
verified: 2026-03-23T00:00:00Z
status: passed
score: 7/7 must-haves verified
---

# Phase 09.1: Coinbase Advanced API Migration Verification Report

**Phase Goal:** Migrate all REST API calls from Coinbase INTX to Coinbase Advanced Trade API -- rewrite auth (HMAC to JWT), all 10 endpoint paths, and response models to match new API shapes
**Verified:** 2026-03-23
**Status:** PASSED
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | JWT auth generates valid ES256 tokens with correct uri claim format | VERIFIED | `libs/coinbase/auth.py`: `jwt.encode(..., algorithm="ES256")`, `uri = f"{method.upper()} {self.BASE_HOST}{path}"` — test_auth.py confirms decoded `uri` == `"GET api.coinbase.com/api/v3/brokerage/products"` |
| 2 | CoinbaseSettings reads from COINBASE_ADV_ prefixed env vars | VERIFIED | `libs/common/config.py` line 24: `env_prefix="COINBASE_ADV_"` — confirmed by `test_coinbase_settings_reads_adv_env_prefix` and `test_env_prefix_is_coinbase_adv` tests |
| 3 | PEM keys with escaped newlines load correctly | VERIFIED | `libs/coinbase/auth.py` line 29: `api_secret.replace("\\n", "\n")` before `load_pem_private_key()` — test suite passes with this normalization |
| 4 | DEFAULT_REST_BASE_URL points to api.coinbase.com | VERIFIED | `libs/common/constants.py` line 41: `DEFAULT_REST_BASE_URL = "https://api.coinbase.com"` — no INTX URL remains |
| 5 | Passphrase fields removed from config and auth | VERIFIED | `CoinbaseAuth.__init__` takes `(api_key, api_secret)` only. `CoinbaseSettings` has no `passphrase_a`/`passphrase_b` fields. `test_passphrase_field_does_not_exist` confirms this. |
| 6 | All 10 REST endpoints use /api/v3/brokerage/ paths with no /api/v1/ paths remaining | VERIFIED | `grep -c "/api/v3/brokerage/"` returns 13 matches; `grep -c "/api/v1/"` returns 0 |
| 7 | Dynamic product ID discovery wired into ingestion startup | VERIFIED | `agents/ingestion/main.py` line 36 imports `discover_and_update_registry`; line 114 calls `await discover_and_update_registry(discovery_client)` before `get_all_instruments()` |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `libs/coinbase/auth.py` | ES256 JWT authentication | VERIFIED | Contains `jwt.encode`, `algorithm="ES256"`, `BASE_HOST = "api.coinbase.com"`, `sign_ws` removed, no passphrase |
| `libs/common/config.py` | COINBASE_ADV_ env prefix | VERIFIED | `env_prefix="COINBASE_ADV_"`, no passphrase fields, `rest_url = "https://api.coinbase.com"` |
| `libs/common/constants.py` | Advanced Trade REST base URL | VERIFIED | `DEFAULT_REST_BASE_URL = "https://api.coinbase.com"`, stale WS URL constants removed |
| `.env.example` | Updated env var documentation | VERIFIED | Contains `COINBASE_ADV_API_KEY_A=`, `COINBASE_ADV_API_SECRET_A=`, no `COINBASE_INTX_` or `PASSPHRASE` |
| `libs/coinbase/models.py` | Advanced Trade response models | VERIFIED | `class Amount(BaseModel)` with `value: str`, `currency: str`; all 9 models use new field shapes; no `InstrumentResponse` |
| `libs/coinbase/rest_client.py` | All 10 Advanced Trade endpoints | VERIFIED | All 10 methods present, 13 `/api/v3/brokerage/` path occurrences, `portfolio_uuid` injected in constructor |
| `libs/coinbase/client_pool.py` | Portfolio UUID injection into clients | VERIFIED | `portfolio_uuid_a` and `portfolio_uuid_b` constructor params, both passed to `CoinbaseRESTClient` |
| `libs/coinbase/product_discovery.py` | Dynamic product ID resolution function | VERIFIED | `discover_product_ids()` and `discover_and_update_registry()` present, calls `get_products(product_type="FUTURE", contract_expiry_type="PERPETUAL")`, filters by `product_venue == "INTX"` |
| `libs/coinbase/tests/test_auth.py` | JWT auth unit tests | VERIFIED | Contains `test_sign_generates_valid_jwt`, `test_pem_newline_normalization`, `jwt.decode` verification |
| `libs/coinbase/tests/test_rest_client.py` | REST endpoint unit tests | VERIFIED | Contains `test_calls_correct_path` for `get_products` (and all 10 endpoints), uses `respx.mock` |
| `libs/coinbase/tests/test_models.py` | Response model unit tests | VERIFIED | Contains `test_position_with_amount`, Amount parsing, FillResponse renamed fields |
| `libs/coinbase/tests/test_product_discovery.py` | Product discovery tests | VERIFIED | 7 tests covering filters, venue matching, missing symbols, registry update |
| `libs/common/tests/test_config_validation.py` | CoinbaseSettings env prefix validation | VERIFIED | Contains `test_coinbase_settings_reads_adv_env_prefix` and passphrase removal tests |
| `libs/common/instruments.py` | InstrumentConfig with resolved_product_id | VERIFIED | `resolved_product_id: str = ""` field, `product_id` property with fallback to `f"{self.id}-INTX"`, `ws_product_id` delegates to `product_id` |
| `agents/ingestion/main.py` | Startup wiring for product discovery | VERIFIED | Imports `discover_and_update_registry`, calls it at startup before `get_all_instruments()`, no passphrase |
| `agents/reconciliation/main.py` | CoinbaseAuth without passphrase | VERIFIED | `CoinbaseAuth(api_key=..., api_secret=...)` only -- no `passphrase` argument in any call |
| `agents/risk/main.py` | CoinbaseAuth without passphrase | VERIFIED | `CoinbaseAuth(settings.coinbase.api_key_a, settings.coinbase.api_secret_a)` -- no passphrase, `CoinbaseClientPool` receives `portfolio_uuid_a/b` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `libs/coinbase/auth.py` | PyJWT library | `jwt.encode` with ES256 algorithm | WIRED | `jwt.encode(jwt_payload, self._private_key, algorithm="ES256", ...)` at line 54 |
| `libs/common/config.py` | environment variables | `env_prefix="COINBASE_ADV_"` in pydantic-settings | WIRED | `SettingsConfigDict(env_prefix="COINBASE_ADV_")` at line 24 |
| `libs/coinbase/rest_client.py` | `libs/coinbase/models.py` | `model_validate` on response data | WIRED | `ProductResponse.model_validate(item)`, `PortfolioResponse.model_validate(summary)`, etc. throughout file |
| `libs/coinbase/rest_client.py` | `libs/coinbase/auth.py` | `self._auth.sign(method, path)` | WIRED | `headers = self._auth.sign(method, path, body_str)` at line 107 |
| `libs/coinbase/client_pool.py` | `libs/coinbase/rest_client.py` | `CoinbaseRESTClient` constructor with `portfolio_uuid` | WIRED | `CoinbaseRESTClient(auth=auth_a, ..., portfolio_uuid=portfolio_uuid_a)` at line 45 |
| `libs/coinbase/product_discovery.py` | `libs/coinbase/rest_client.py` | `client.get_products()` call | WIRED | `await client.get_products(product_type="FUTURE", contract_expiry_type="PERPETUAL")` at line 36 |
| `libs/coinbase/product_discovery.py` | `libs/common/instruments.py` | `update_registry_product_ids()` call | WIRED | `update_registry_product_ids(mapping)` at line 86 |
| `agents/ingestion/main.py` | `libs/coinbase/product_discovery.py` | `await discover_and_update_registry()` at startup | WIRED | Import at line 36, call at line 114 before `get_all_instruments()` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| MIG-01 | 09.1-01 | JWT auth generates valid ES256 tokens | SATISFIED | `auth.py` ES256 implementation; `test_auth.py` 47 tests all pass |
| MIG-02 | 09.1-02 | All 10 endpoints use correct Advanced Trade paths | SATISFIED | All 10 methods use `/api/v3/brokerage/` paths; 0 `/api/v1/` paths remain |
| MIG-03 | 09.1-02 | Response models parse Advanced Trade JSON shapes | SATISFIED | `models.py` fully rewritten: `Amount` objects, string fields, renamed fields (`entry_id`, `commission`, `product_id`) |
| MIG-04 | 09.1-04 | Product ID discovery resolves perp contracts | SATISFIED | `product_discovery.py` exists with FUTURE+PERPETUAL filter, INTX venue filter; 7 tests pass |
| MIG-05 | 09.1-01, 09.1-03 | CoinbaseSettings uses COINBASE_ADV_ env prefix | SATISFIED | `env_prefix="COINBASE_ADV_"` in `config.py`; `test_config_validation.py` covers this specifically |
| MIG-06 | 09.1-01 | PEM key with escaped newlines loads correctly | SATISFIED | `api_secret.replace("\\n", "\n")` in `auth.py`; `test_pem_newline_normalization` test passes |
| MIG-07 | 09.1-03, 09.1-04 | Existing agents still work after migration | SATISFIED | All 766 agent tests pass; `reconciliation/main.py` and `risk/main.py` have no passphrase references; `ingestion/main.py` has no passphrase references |

**No orphaned requirements.** The ROADMAP lists exactly MIG-01 through MIG-07 for this phase. All 7 are claimed across plans and verified in the codebase.

### Anti-Patterns Found

No blockers or warnings found. Scan results:
- No TODO/FIXME/XXX/HACK/PLACEHOLDER comments in any modified file
- No stub return patterns (`return null`, `return {}`, `return []`)
- No console.log-only implementations
- One intentional `# Advanced Trade reduce-only via leverage` comment in `rest_client.py` -- this is a legitimate explanatory comment, not a placeholder

### Human Verification Required

None. All phase requirements are verifiable through code inspection and automated tests.

The following items are noted as requiring live integration testing before production use (outside this phase's scope):
1. **Live JWT acceptance by Coinbase Advanced Trade API** -- requires real credentials to test end-to-end token acceptance. Tests use locally-generated EC keys and verify token structure, but cannot verify Coinbase's server-side JWT validation.
2. **Funding rate field path** -- `future_product_details.perpetual_details.funding_rate` is derived from research; actual field path from live API response needs validation.
3. **Portfolio UUID path format** -- `/api/v3/brokerage/intx/portfolio/{uuid}` is documented in research; live API call needed to confirm the response shape matches `PortfolioResponse`.

These are operational concerns, not gaps in the phase deliverables.

### Gaps Summary

No gaps. All phase deliverables are present, substantive, and wired:

- Auth layer fully migrated: HMAC-SHA256 replaced with ES256 JWT, passphrase removed everywhere
- All 10 endpoint methods rewritten: correct Advanced Trade paths, no INTX `/api/v1/` paths remain
- Response models updated: `Amount` objects, string numeric fields, renamed keys (`product_id`, `commission`, `entry_id`)
- Portfolio UUID flows end-to-end: `CoinbaseSettings` -> `PortfolioIDSettings` -> `CoinbaseClientPool` -> `CoinbaseRESTClient._portfolio_uuid`
- Product discovery wired: ingestion agent calls `discover_and_update_registry()` at startup, before building WS subscription mappings
- All 47 coinbase library tests pass, all 766 agent regression tests pass

---

_Verified: 2026-03-23_
_Verifier: Claude (gsd-verifier)_
