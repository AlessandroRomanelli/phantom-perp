---
phase: 9.1
slug: coinbase-advanced-api-migration
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-23
---

# Phase 9.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x with pytest-asyncio |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `python -m pytest libs/coinbase/tests/ -x -q` |
| **Full suite command** | `python -m pytest libs/coinbase/tests/ agents/ingestion/tests/ agents/execution/tests/ agents/reconciliation/tests/ -x -q` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `python -m pytest libs/coinbase/tests/ -x -q`
- **After every plan wave:** Run `python -m pytest libs/coinbase/tests/ agents/ingestion/tests/ agents/execution/tests/ agents/reconciliation/tests/ -x -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | TBD | unit | TBD | TBD | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*To be populated after plans are created.*

---

## Wave 0 Requirements

- [ ] `libs/coinbase/tests/test_auth.py` — JWT signing tests (new auth mechanism)
- [ ] `libs/coinbase/tests/test_rest_client.py` — Advanced Trade endpoint tests with mocked responses

*Existing test infrastructure covers framework needs.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live API connectivity | N/A | Requires real API credentials | Deploy to paper environment, verify candle/funding data flows |
| Product ID resolution | D-14 | Requires live instruments endpoint | Run ingestion agent, verify all 5 instruments resolve |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
