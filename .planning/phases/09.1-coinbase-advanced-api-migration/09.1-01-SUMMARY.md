---
phase: 09.1-coinbase-advanced-api-migration
plan: 01
subsystem: auth
tags: [jwt, es256, pyjwt, cryptography, coinbase-advanced-trade]

# Dependency graph
requires:
  - phase: none
    provides: existing HMAC auth module being replaced
provides:
  - ES256 JWT authentication module for Coinbase Advanced Trade API
  - COINBASE_ADV_ env prefix in config model
  - Updated REST base URL to api.coinbase.com
  - PyJWT and cryptography dependencies
affects: [09.1-02, 09.1-03, 09.1-04]

# Tech tracking
tech-stack:
  added: [PyJWT>=2.8, cryptography>=42]
  patterns: [ES256 JWT with uri claim, PEM newline normalization for env vars]

key-files:
  created: []
  modified:
    - pyproject.toml
    - libs/coinbase/auth.py
    - libs/common/config.py
    - libs/common/constants.py
    - configs/default.yaml
    - .env.example

key-decisions:
  - "PEM newline normalization via replace('\\n', '\\n') handles env var escaping"
  - "Removed sign_ws() method entirely -- WS auth handled separately in later plan"
  - "Removed stale DEFAULT_WS_MARKET_URL and DEFAULT_WS_USER_URL constants (unused)"

patterns-established:
  - "JWT auth: fresh token per request with 2-min lifetime, no caching needed"
  - "URI claim format: '{METHOD} api.coinbase.com{path}'"

requirements-completed: [MIG-01, MIG-05, MIG-06]

# Metrics
duration: 2min
completed: 2026-03-23
---

# Phase 09.1 Plan 01: Auth + Config Foundation Summary

**ES256 JWT auth replacing HMAC-SHA256, COINBASE_ADV_ env prefix, api.coinbase.com base URL, PyJWT + cryptography deps**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-23T14:08:08Z
- **Completed:** 2026-03-23T14:10:30Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Rewrote CoinbaseAuth from HMAC-SHA256 to ES256 JWT with PyJWT library
- Updated config model to COINBASE_ADV_ env prefix, removed passphrase fields
- Updated all URLs from api.international.coinbase.com to api.coinbase.com
- Cleaned up stale WS URL constants and default.yaml entries

## Task Commits

Each task was committed atomically:

1. **Task 1: Add PyJWT + cryptography deps and rewrite auth.py to ES256 JWT** - `370654f` (feat)
2. **Task 2: Update config.py env prefix, constants.py URLs, and .env.example** - `bb89a2e` (feat)

## Files Created/Modified
- `pyproject.toml` - Added PyJWT>=2.8 and cryptography>=42 dependencies
- `libs/coinbase/auth.py` - Complete rewrite from HMAC to ES256 JWT generation
- `libs/common/config.py` - Changed env_prefix to COINBASE_ADV_, removed passphrase fields
- `libs/common/constants.py` - Updated REST URL, removed stale WS URL constants
- `configs/default.yaml` - Updated rest_base_url, removed stale ws URLs
- `.env.example` - Rewritten with COINBASE_ADV_* variable names and PEM key documentation

## Decisions Made
- Removed sign_ws() method entirely since WS auth is handled in a later plan
- Removed DEFAULT_WS_MARKET_URL and DEFAULT_WS_USER_URL constants (no consumers found in codebase)
- Kept body parameter in sign() for interface compatibility even though JWT doesn't sign the body

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Auth module ready for REST client endpoint migration (Plan 02)
- Config model ready for all agents to read COINBASE_ADV_* env vars
- Note: callers still pass passphrase= to CoinbaseAuth constructor -- will be fixed in Plan 04 (caller migration)

## Self-Check: PASSED

All files exist, all commits verified.

---
*Phase: 09.1-coinbase-advanced-api-migration*
*Completed: 2026-03-23*
