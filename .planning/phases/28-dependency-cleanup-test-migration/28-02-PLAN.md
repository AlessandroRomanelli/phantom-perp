---
phase: 28-dependency-cleanup-test-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/signals/tests/test_claude_market_analysis.py
  - agents/signals/tests/test_orch_client.py
autonomous: true
requirements:
  - DEP-03

must_haves:
  truths:
    - "test_claude_market_analysis.py passes without the anthropic package installed"
    - "test_orch_client.py passes without the anthropic package installed"
    - "No test file under agents/signals/tests/ imports or references the anthropic SDK"
  artifacts:
    - path: "agents/signals/tests/test_claude_market_analysis.py"
      provides: "Market analysis tests mocking asyncio.create_subprocess_exec"
    - path: "agents/signals/tests/test_orch_client.py"
      provides: "Orchestrator client tests mocking asyncio.create_subprocess_exec"
  key_links:
    - from: "agents/signals/tests/test_claude_market_analysis.py"
      to: "agents/signals/claude_market_client.py"
      via: "import call_claude_analysis, build_market_context, build_system_prompt, validate_claude_response"
      pattern: "from agents.signals.claude_market_client import"
    - from: "agents/signals/tests/test_orch_client.py"
      to: "agents/signals/orch_client.py"
      via: "import call_claude_orchestrator, build_orchestrator_context, validate_orchestrator_response, OrchestratorParams"
      pattern: "from agents.signals.orch_client import"
---

<objective>
Migrate the two async signal test files from mocking the Anthropic SDK to mocking asyncio.create_subprocess_exec, matching the Phase 27 production code migration.

Purpose: Completes DEP-03 for the signals side — all three call site test files will mock subprocess instead of the Anthropic SDK, enabling the full test suite to pass without the anthropic package.

Output: Rewritten test_claude_market_analysis.py and test_orch_client.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@agents/signals/claude_market_client.py
@agents/signals/orch_client.py
@agents/signals/tests/test_claude_market_analysis.py
@agents/signals/tests/test_orch_client.py

<interfaces>
<!-- Production code after Phase 27 migration -->

From agents/signals/claude_market_client.py:
```python
_CLI_TIMEOUT_SECONDS: int = 90

def build_system_prompt() -> str: ...
def build_market_context(instrument_id, store, snapshot, regime) -> str: ...
def validate_claude_response(raw, snapshot, store) -> dict | None: ...

async def call_claude_analysis(
    instrument_id: str,
    store: FeatureStore,
    snapshot: MarketSnapshot,
    regime: MarketRegime,
) -> dict[str, Any] | None: ...
```

call_claude_analysis now uses asyncio.create_subprocess_exec("claude", "-p", full_prompt).
Returns None on: TimeoutError, OSError, non-zero returncode, JsonExtractionError, non-dict.
Parses response via extract_json(stdout_text), validates via validate_claude_response().

MARKET_ANALYSIS_TOOL no longer exists — removed in Phase 27.
System prompt no longer references "submit_market_analysis" — it now instructs JSON code block output.

From agents/signals/orch_client.py:
```python
_CLI_TIMEOUT_SECONDS: int = 120

class OrchestratorParams: ...
def _build_orchestrator_system_prompt() -> str: ...
def build_orchestrator_context(...) -> str: ...

async def call_claude_orchestrator(
    context_str: str,
    params: OrchestratorParams,
) -> list[dict[str, Any]] | None: ...

def validate_orchestrator_response(
    decisions: list[dict[str, Any]],
    bounds_path: Path = _DEFAULT_BOUNDS_PATH,
) -> list[dict[str, Any]]: ...
```

call_claude_orchestrator now uses asyncio.create_subprocess_exec("claude", "-p", full_prompt).
Returns None on: TimeoutError, OSError, non-zero returncode, JsonExtractionError, non-dict.
Parses response via extract_json(stdout_text), extracts decisions list.
No longer checks ANTHROPIC_API_KEY — CLI auth is handled by the claude binary.
No longer logs input_tokens/output_tokens (no SDK usage object).

ORCHESTRATOR_TOOL no longer exists — removed in Phase 27.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite test_claude_market_analysis.py to mock subprocess</name>
  <files>agents/signals/tests/test_claude_market_analysis.py</files>
  <read_first>
    - agents/signals/tests/test_claude_market_analysis.py (full file — all 5 test classes)
    - agents/signals/claude_market_client.py (production code — understand subprocess pattern)
    - libs/common/json_extractor.py (understand extract_json for crafting mock stdout)
  </read_first>
  <action>
Modify agents/signals/tests/test_claude_market_analysis.py to remove all Anthropic SDK dependencies.

**Remove:**
- `import anthropic` (line 16)
- `import httpx` (line 17) — was only used for fake httpx.Request in API error test
- `MARKET_ANALYSIS_TOOL` from the import list (line 21)
- `_make_tool_use_block()` helper (lines 97-102)
- `_make_claude_response()` helper (lines 105-115)

**Keep unchanged (no anthropic dependency):**
- TestClaudeMarketAnalysisStrategy class (6 tests) — tests queue bridge, no SDK
- TestValidateClaudeResponse class (8 tests) — tests validation logic, no SDK
- TestBuildMarketContext class — tests 1-3 (context_contains_expected_sections, context_with_minimal_store, build_system_prompt_returns_string)
- TestProcessInstrument class (9 tests) — mocks call_claude_analysis at scheduler level, no SDK

**Remove entirely:**
- test_market_analysis_tool_schema (line 471-476) in TestBuildMarketContext — MARKET_ANALYSIS_TOOL no longer exists

**Fix:**
- test_build_system_prompt_returns_string (line 464-469): Change assertion from `assert "submit_market_analysis" in prompt` to `assert "JSON" in prompt` — the production prompt now instructs JSON code block output, not tool use

**Rewrite TestCallClaudeAnalysis (5 tests) to mock asyncio.create_subprocess_exec:**

Add helper to create mock subprocess output:
```python
import json

def _mock_cli_response(raw: dict[str, Any]) -> bytes:
    """Build mock CLI stdout with JSON in a code block."""
    return f'```json\n{json.dumps(raw)}\n```'.encode()
```

For `test_valid_long_response_returns_signal_dict`:
- Create an `AsyncMock` for `asyncio.create_subprocess_exec` that returns a mock process
- Mock process: returncode=0, communicate() returns (_mock_cli_response(raw_response), b"")
- Patch `asyncio.create_subprocess_exec` (not anthropic.AsyncAnthropic)
- Remove `patch.dict("os.environ", {"ANTHROPIC_API_KEY": ...})` — no longer needed
- Keep all assertions unchanged

For `test_no_signal_direction_returns_no_signal_dict`:
- Same subprocess mock pattern
- Keep all assertions unchanged

For `test_missing_api_key_returns_none` — REPLACE with `test_cli_timeout_returns_none`:
- Patch `asyncio.create_subprocess_exec` to return a mock process
- Make communicate() raise `asyncio.TimeoutError`
- Assert result is None

For `test_api_error_returns_none_and_does_not_raise` — REPLACE with `test_cli_nonzero_exit_returns_none`:
- Mock process with returncode=1, stderr=b"error"
- Assert result is None, no exception raised

For `test_missing_tool_use_block_returns_none` — REPLACE with `test_cli_invalid_json_returns_none`:
- Mock process with returncode=0, stdout=b"I cannot help you with that."
- Assert result is None (extract_json will raise JsonExtractionError)

Add new test: `test_cli_oserror_returns_none`:
- Patch asyncio.create_subprocess_exec to raise OSError("claude not found")
- Assert result is None
  </action>
  <verify>
    <automated>cd /Users/alessandroromanelli/Projects/phantom-perp && python -m pytest agents/signals/tests/test_claude_market_analysis.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - No `import anthropic` in the file
    - No `import httpx` in the file
    - No `MARKET_ANALYSIS_TOOL` import or reference
    - No `_make_tool_use_block` or `_make_claude_response` helpers
    - No `patch("agents.signals.claude_market_client.anthropic.AsyncAnthropic")` patterns
    - No `patch.dict("os.environ", {"ANTHROPIC_API_KEY": ...})` patterns
    - All tests pass: pytest exits 0
    - Test count is >= 28 (was ~30 minus 2 removed + 1 added)
    - TestClaudeMarketAnalysisStrategy tests unchanged
    - TestValidateClaudeResponse tests unchanged
    - TestProcessInstrument tests unchanged
  </acceptance_criteria>
  <done>
    - agents/signals/tests/test_claude_market_analysis.py has zero references to anthropic SDK
    - All tests pass: pytest agents/signals/tests/test_claude_market_analysis.py exits 0
    - Coverage of call_claude_analysis includes: success, no_signal, timeout, nonzero exit, bad JSON, OSError
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_orch_client.py to mock subprocess</name>
  <files>agents/signals/tests/test_orch_client.py</files>
  <read_first>
    - agents/signals/tests/test_orch_client.py (full file — all test classes)
    - agents/signals/orch_client.py (production code — understand subprocess pattern)
  </read_first>
  <action>
Modify agents/signals/tests/test_orch_client.py to remove all Anthropic SDK dependencies.

**Remove:**
- `ORCHESTRATOR_TOOL` from the import list (line 21)
- `_make_tool_use_block()` helper (lines 92-97) — no longer needed
- `_make_claude_response()` helper (lines 100-110) — no longer needed

**Remove entirely (classes testing removed constants/SDK behavior):**
- TestOrchestratorTool class (7 tests, lines 132-172) — ORCHESTRATOR_TOOL no longer exists
- TestOrchestratorToolConfidenceSchema class (3 tests, lines 685-701) — schema for removed constant
- TestCallClaudeOrchestratorTokenLogging class (1 test, lines 732-762) — tested SDK usage.input_tokens/output_tokens which no longer exist in CLI path

**Keep unchanged (no anthropic dependency):**
- All shared helpers (_snap, _build_store, _make_regime_detector — no SDK dependency)
- TestBuildOrchestratorContext class (10 tests) — tests context assembly, no SDK
- TestValidateOrchestratorResponse class (12 tests) — tests bounds clipping, no SDK
- TestOrchestratorParams class (4 tests) — tests dataclass, no SDK
- TestOrchestratorParamsT02 class (4 tests) — tests params defaults, no SDK
- TestValidateOrchestratorResponseT02 class (4 tests) — tests confidence pass-through, no SDK
- TestBuildOrchestratorContextNewsSection class (6 tests) — tests news context, no SDK

**Rewrite TestCallClaudeOrchestrator (5 tests) to mock asyncio.create_subprocess_exec:**

Add helper:
```python
import json

def _mock_cli_response(decisions: list[dict[str, Any]], summary: str = "ok") -> bytes:
    """Build mock CLI stdout with orchestrator JSON in a code block."""
    payload = {"decisions": decisions, "summary": summary}
    return f'```json\n{json.dumps(payload)}\n```'.encode()
```

For `test_valid_response_returns_decisions`:
- Mock asyncio.create_subprocess_exec to return a process with:
  - returncode=0
  - communicate() returns (_mock_cli_response(decisions), b"")
- Remove `patch("agents.signals.orch_client.anthropic.AsyncAnthropic")`
- Remove `patch.dict("os.environ", {"ANTHROPIC_API_KEY": "test-key"})`
- Keep all assertions unchanged

For `test_missing_api_key_returns_none` — REPLACE with `test_cli_timeout_returns_none`:
- Mock asyncio.create_subprocess_exec process whose communicate() raises asyncio.TimeoutError
- Assert result is None

For `test_api_error_returns_none` — REPLACE with `test_cli_nonzero_exit_returns_none`:
- Mock process with returncode=1, stderr=b"error"
- Remove `import anthropic as anth` (line 354)
- Assert result is None

For `test_missing_tool_use_block_returns_none` — REPLACE with `test_cli_invalid_json_returns_none`:
- Mock process with returncode=0, stdout=b"Sorry, I cannot help."
- Assert result is None

For `test_empty_decisions_list_returned`:
- Mock subprocess with stdout=_mock_cli_response([], summary="No changes needed.")
- Remove SDK mocking
- Keep assertion: result == []
  </action>
  <verify>
    <automated>cd /Users/alessandroromanelli/Projects/phantom-perp && python -m pytest agents/signals/tests/test_orch_client.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - No `import anthropic` anywhere in the file
    - No `ORCHESTRATOR_TOOL` import or reference
    - No `_make_tool_use_block` or `_make_claude_response` helpers
    - No `patch("agents.signals.orch_client.anthropic.AsyncAnthropic")` patterns
    - No `patch.dict("os.environ", {"ANTHROPIC_API_KEY": ...})` patterns
    - All tests pass: pytest exits 0
    - TestOrchestratorTool class removed (7 tests)
    - TestOrchestratorToolConfidenceSchema class removed (3 tests)
    - TestCallClaudeOrchestratorTokenLogging class removed (1 test)
    - Test count is >= 35 (was ~46 minus 11 removed)
    - TestBuildOrchestratorContext tests unchanged
    - TestValidateOrchestratorResponse tests unchanged
    - TestOrchestratorParams tests unchanged
    - TestBuildOrchestratorContextNewsSection tests unchanged
  </acceptance_criteria>
  <done>
    - agents/signals/tests/test_orch_client.py has zero references to anthropic SDK
    - All tests pass: pytest agents/signals/tests/test_orch_client.py exits 0
    - Coverage of call_claude_orchestrator includes: success, timeout, nonzero exit, bad JSON, empty decisions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — test-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Repudiation | test files | accept | Tests are not production code; no security impact from mocking strategy changes |
</threat_model>

<verification>
1. `grep -r "import anthropic" agents/signals/tests/test_claude_market_analysis.py agents/signals/tests/test_orch_client.py` returns no matches
2. `grep -r "ANTHROPIC_API_KEY" agents/signals/tests/test_claude_market_analysis.py agents/signals/tests/test_orch_client.py` returns no matches
3. `grep -r "MARKET_ANALYSIS_TOOL\|ORCHESTRATOR_TOOL" agents/signals/tests/test_claude_market_analysis.py agents/signals/tests/test_orch_client.py` returns no matches
4. `python -m pytest agents/signals/tests/test_claude_market_analysis.py agents/signals/tests/test_orch_client.py -x` passes all tests
</verification>

<success_criteria>
- Both test files have zero references to anthropic SDK (DEP-03 — signals side)
- Both test files mock asyncio.create_subprocess_exec
- All tests pass
- Non-SDK test classes are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/28-dependency-cleanup-test-migration/28-02-SUMMARY.md`
</output>
