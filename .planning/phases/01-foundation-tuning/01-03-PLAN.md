---
phase: 01-foundation-tuning
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - configs/strategy_matrix.yaml
  - configs/strategies/mean_reversion.yaml
  - configs/strategies/correlation.yaml
  - configs/strategies/liquidation_cascade.yaml
  - configs/strategies/regime_trend.yaml
  - agents/signals/main.py
  - agents/signals/tests/test_instrument_configs.py
autonomous: true
requirements: [TUNE-01, TUNE-02, TUNE-03, TUNE-04, TUNE-05]

must_haves:
  truths:
    - "ETH-PERP loads its own parameter overrides from each strategy YAML"
    - "BTC-PERP loads its own parameter overrides from each strategy YAML"
    - "SOL-PERP loads its own parameter overrides from each strategy YAML"
    - "QQQ-PERP loads its own parameter overrides from each strategy YAML"
    - "SPY-PERP loads its own parameter overrides from each strategy YAML"
    - "Strategy matrix declares which strategies run on which instruments"
    - "Momentum stays globally disabled (D-09)"
    - "Thresholds are lowered to increase signal frequency (D-04)"
  artifacts:
    - path: "configs/strategy_matrix.yaml"
      provides: "Explicit strategy-instrument enablement matrix"
      contains: "strategies:"
    - path: "configs/strategies/mean_reversion.yaml"
      provides: "Per-instrument overrides for all 5 instruments"
      contains: "instruments:"
    - path: "configs/strategies/correlation.yaml"
      provides: "Per-instrument overrides for all 5 instruments"
      contains: "instruments:"
    - path: "configs/strategies/liquidation_cascade.yaml"
      provides: "Per-instrument overrides for crypto instruments"
      contains: "ETH-PERP:"
    - path: "configs/strategies/regime_trend.yaml"
      provides: "Updated per-instrument overrides including ETH-PERP"
      contains: "ETH-PERP:"
    - path: "agents/signals/tests/test_instrument_configs.py"
      provides: "Tests verifying all instruments load configs correctly"
      contains: "test_all_instruments_have_overrides"
  key_links:
    - from: "configs/strategy_matrix.yaml"
      to: "agents/signals/main.py"
      via: "main.py loads matrix to determine per-instrument enablement"
      pattern: "strategy_matrix"
    - from: "configs/strategies/mean_reversion.yaml"
      to: "libs/common/config.py"
      via: "load_strategy_config_for_instrument merges overrides"
      pattern: "load_strategy_config_for_instrument"
---

<objective>
Create the strategy matrix and add per-instrument parameter overrides for all 5 instruments across all active strategies (mean_reversion, liquidation_cascade, correlation, regime_trend). Momentum stays disabled per D-09.

Purpose: Every instrument trades with asset-appropriate thresholds instead of universal defaults. ETH gets crypto-vol parameters, BTC gets higher-liquidity parameters, SOL gets high-vol/thin-book parameters, QQQ/SPY get equity-perp parameters. Thresholds are actively lowered per D-04 to increase signal frequency.

Output: strategy_matrix.yaml, updated strategy YAML files with per-instrument overrides, integration in main.py, config loading tests.
</objective>

<execution_context>
@/Users/aroman/Work/phantom-perp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aroman/Work/phantom-perp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-tuning/01-CONTEXT.md
@.planning/phases/01-foundation-tuning/01-RESEARCH.md
@.planning/phases/01-foundation-tuning/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From libs/common/config.py (after Plan 02):
```python
def load_strategy_config(strategy_name: str) -> dict[str, Any]: ...
def load_strategy_config_for_instrument(strategy_name: str, instrument_id: str) -> dict[str, Any]: ...
def validate_strategy_config(strategy_name: str, config: dict[str, Any], params_cls: type) -> None: ...
def log_config_diff(strategy_name: str, instrument_id: str, merged_params: dict[str, Any], default_params: dict[str, Any]) -> None: ...
```

From agents/signals/main.py (after Plan 02):
```python
STRATEGY_CLASSES: dict[str, type[SignalStrategy]] = {
    "momentum": MomentumStrategy,
    "mean_reversion": MeanReversionStrategy,
    "liquidation_cascade": LiquidationCascadeStrategy,
    "correlation": CorrelationStrategy,
    "regime_trend": RegimeTrendStrategy,
}

STRATEGY_PARAMS_CLASSES: dict[str, type] = {
    "momentum": MomentumParams,
    "mean_reversion": MeanReversionParams,
    "liquidation_cascade": LiquidationCascadeParams,
    "correlation": CorrelationParams,
    "regime_trend": RegimeTrendParams,
}

def build_strategies_for_instrument(instrument_id: str) -> list[SignalStrategy]:
    # Now includes validate_strategy_config and log_config_diff calls
```

Reference YAML pattern (regime_trend.yaml):
```yaml
strategy:
  name: "regime_trend"
  enabled: true
  weight: 0.30
parameters:
  trend_ema_period: 50
  # ...
instruments:
  BTC-PERP:
    parameters:
      trend_ema_period: 60
```

Existing strategy Params fields:
- MeanReversionParams: bb_period, bb_std, rsi_period, rsi_overbought, rsi_oversold, adx_period, adx_max, atr_period, stop_loss_atr_mult, min_conviction, cooldown_bars
- LiquidationCascadeParams: oi_lookback, oi_drop_threshold_pct, imbalance_threshold, vol_spike_mult, atr_period, stop_loss_atr_mult, take_profit_atr_mult, min_conviction, cooldown_bars
- CorrelationParams: basis_lookback, basis_zscore_threshold, oi_divergence_lookback, oi_divergence_threshold_pct, atr_period, stop_loss_atr_mult, take_profit_atr_mult, min_conviction, cooldown_bars
- RegimeTrendParams: (already has per-instrument overrides for BTC, SOL, QQQ, SPY -- need to add ETH-PERP)

Instrument characteristics (per D-01, D-02, D-03):
- ETH-PERP: Mid volatility, good liquidity, 24/7 crypto, most paper-traded (too few signals per D-04)
- BTC-PERP: Lower volatility than ETH, highest liquidity, 24/7 crypto
- SOL-PERP: High volatility, thin orderbook, 24/7 crypto
- QQQ-PERP: Equity index perp, lower vol, 24/7 but volume concentrates US hours (no session gating per D-12)
- SPY-PERP: Equity index perp, lowest vol of all, 24/7 but volume concentrates US hours (no session gating per D-12)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create strategy matrix and integrate into main.py</name>
  <files>configs/strategy_matrix.yaml, agents/signals/main.py</files>
  <read_first>
    - agents/signals/main.py
    - configs/strategies/momentum.yaml
    - configs/strategies/mean_reversion.yaml
    - configs/strategies/liquidation_cascade.yaml
    - configs/strategies/correlation.yaml
    - configs/strategies/regime_trend.yaml
  </read_first>
  <action>
1. Create `configs/strategy_matrix.yaml` with the following content. Decisions per D-09 (momentum disabled), D-11 (liquidation cascade disabled for QQQ/SPY -- crypto-native phenomenon), and Claude's discretion (correlation enabled for QQQ/SPY because basis divergence is valid for equity perps):

```yaml
# Strategy Matrix — which strategies run on which instruments.
# This is the single source of truth for per-instrument enablement.
# Individual strategy YAMLs control global enabled/disabled state;
# this matrix controls per-instrument enablement within that.

strategies:
  momentum:
    enabled: false  # Globally disabled until Phase 2 (D-09)
    instruments: []

  mean_reversion:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      - QQQ-PERP
      - SPY-PERP

  liquidation_cascade:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      # QQQ/SPY disabled: liquidation cascades are crypto-native (D-11)

  correlation:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      - QQQ-PERP
      - SPY-PERP
      # QQQ/SPY enabled: basis divergence valid for equity perps

  regime_trend:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      - QQQ-PERP
      - SPY-PERP
```

2. Update `agents/signals/main.py` to load and use the strategy matrix:
   - Add a new function `load_strategy_matrix()` that reads `configs/strategy_matrix.yaml`:
     ```python
     def load_strategy_matrix() -> dict[str, Any]:
         """Load the strategy-instrument enablement matrix."""
         matrix_path = Path(__file__).resolve().parent.parent.parent / "configs" / "strategy_matrix.yaml"
         if not matrix_path.exists():
             return {}
         with open(matrix_path) as f:
             return yaml.safe_load(f) or {}
     ```
   - Add imports: `from pathlib import Path` and `import yaml`
   - In `build_strategies_for_instrument()`, after loading config and before checking enabled status, check the matrix:
     ```python
     matrix = load_strategy_matrix()
     matrix_entry = matrix.get("strategies", {}).get(strategy_name, {})

     # Matrix global toggle overrides per-strategy YAML
     if not matrix_entry.get("enabled", True):
         continue

     # Matrix instrument list controls per-instrument enablement
     matrix_instruments = matrix_entry.get("instruments", [])
     if matrix_instruments and instrument_id not in matrix_instruments:
         continue
     ```
   - Place this check BEFORE the existing `strategy_meta.get("enabled", True)` check so both the matrix and the per-strategy YAML must agree for a strategy to run.
  </action>
  <verify>
    <automated>/Users/aroman/Work/phantom-perp/.venv/bin/python -c "
import yaml
from pathlib import Path
m = yaml.safe_load(open('configs/strategy_matrix.yaml'))
strats = m['strategies']
assert strats['momentum']['enabled'] == False
assert 'ETH-PERP' in strats['mean_reversion']['instruments']
assert 'QQQ-PERP' not in strats['liquidation_cascade']['instruments']
assert 'QQQ-PERP' in strats['correlation']['instruments']
print('Matrix OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - configs/strategy_matrix.yaml exists and contains `strategies:` top-level key
    - Matrix has momentum.enabled = false
    - Matrix has mean_reversion.instruments containing all 5 instruments
    - Matrix has liquidation_cascade.instruments containing only ETH-PERP, BTC-PERP, SOL-PERP (not QQQ/SPY)
    - Matrix has correlation.instruments containing all 5 instruments
    - Matrix has regime_trend.instruments containing all 5 instruments
    - main.py contains `def load_strategy_matrix(`
    - main.py contains `matrix_instruments` check in build_strategies_for_instrument
  </acceptance_criteria>
  <done>Strategy matrix created declaring explicit strategy-instrument enablement. main.py loads the matrix during strategy construction. Momentum globally disabled (D-09). Liquidation cascade disabled for QQQ/SPY (D-11). Correlation enabled for QQQ/SPY (basis divergence valid).</done>
</task>

<task type="auto">
  <name>Task 2: Add per-instrument overrides to all active strategy configs</name>
  <files>configs/strategies/mean_reversion.yaml, configs/strategies/correlation.yaml, configs/strategies/liquidation_cascade.yaml, configs/strategies/regime_trend.yaml</files>
  <read_first>
    - configs/strategies/mean_reversion.yaml
    - configs/strategies/correlation.yaml
    - configs/strategies/liquidation_cascade.yaml
    - configs/strategies/regime_trend.yaml
    - agents/signals/strategies/mean_reversion.py (lines 38-52)
    - agents/signals/strategies/correlation.py (lines 34-46)
    - agents/signals/strategies/liquidation_cascade.py (lines 33-45)
    - agents/signals/strategies/regime_trend.py (lines 40-77)
  </read_first>
  <action>
**Per D-04: Actively lower thresholds to increase signal frequency.** The current paper trading on ETH showed too little activity, especially on weekends. Each instrument's parameters are research-informed per D-01, treated independently per D-02, with crypto vs equity fully separate per D-03. No session logic per D-12. Single profile per instrument per D-14.

1. **mean_reversion.yaml** -- Replace entire file with per-instrument overrides. Base defaults are already in the file. Add `instruments:` section. Key tuning principle: lower min_conviction and widen bands to get more signals.

```yaml
strategy:
  name: "mean_reversion"
  enabled: true
  weight: 0.15

parameters:
  bb_period: 20
  bb_std: 2.0
  rsi_period: 14
  rsi_oversold: 30
  rsi_overbought: 70
  adx_period: 14
  adx_max: 40.0
  atr_period: 14
  stop_loss_atr_mult: 1.5
  min_conviction: 0.35
  cooldown_bars: 8

instruments:
  ETH-PERP:
    parameters:
      # ETH: Mid-vol, good mean reversion. Lower thresholds for more signals (D-04)
      bb_std: 1.8
      rsi_oversold: 32
      rsi_overbought: 68
      adx_max: 35.0
      min_conviction: 0.30
      cooldown_bars: 6

  BTC-PERP:
    parameters:
      # BTC: Lower vol than ETH, tighter bands work, longer cooldown
      bb_period: 25
      bb_std: 1.8
      rsi_oversold: 33
      rsi_overbought: 67
      adx_max: 35.0
      min_conviction: 0.30
      cooldown_bars: 8

  SOL-PERP:
    parameters:
      # SOL: High vol, wider bands needed, faster cooldown for more activity
      bb_std: 2.2
      rsi_oversold: 28
      rsi_overbought: 72
      adx_max: 45.0
      stop_loss_atr_mult: 2.0
      min_conviction: 0.30
      cooldown_bars: 5

  QQQ-PERP:
    parameters:
      # QQQ: Equity perp, lower vol, tighter bands. 24/7 active (D-13, D-15)
      bb_period: 25
      bb_std: 1.5
      rsi_oversold: 35
      rsi_overbought: 65
      adx_max: 30.0
      min_conviction: 0.30
      cooldown_bars: 10

  SPY-PERP:
    parameters:
      # SPY: Lowest vol equity perp, tightest bands
      bb_period: 25
      bb_std: 1.5
      rsi_oversold: 35
      rsi_overbought: 65
      adx_max: 28.0
      min_conviction: 0.30
      cooldown_bars: 10
```

2. **correlation.yaml** -- Add instruments section:

```yaml
strategy:
  name: "correlation"
  enabled: true
  weight: 0.10

parameters:
  basis_lookback: 60
  basis_zscore_threshold: 2.0
  oi_divergence_lookback: 20
  oi_divergence_threshold_pct: 1.5
  atr_period: 14
  stop_loss_atr_mult: 2.0
  take_profit_atr_mult: 3.0
  min_conviction: 0.35
  cooldown_bars: 12

instruments:
  ETH-PERP:
    parameters:
      # ETH: Good basis dynamics, lower thresholds for more signals (D-04)
      basis_zscore_threshold: 1.8
      oi_divergence_threshold_pct: 1.2
      min_conviction: 0.30
      cooldown_bars: 10

  BTC-PERP:
    parameters:
      # BTC: Higher liquidity = tighter basis, need lower z-score threshold
      basis_lookback: 80
      basis_zscore_threshold: 1.7
      oi_divergence_threshold_pct: 1.0
      min_conviction: 0.30
      cooldown_bars: 12

  SOL-PERP:
    parameters:
      # SOL: Wider basis swings, can use higher thresholds but lower conviction
      basis_zscore_threshold: 2.0
      oi_divergence_threshold_pct: 2.0
      stop_loss_atr_mult: 2.5
      take_profit_atr_mult: 4.0
      min_conviction: 0.30
      cooldown_bars: 8

  QQQ-PERP:
    parameters:
      # QQQ: Equity perp basis reflects index tracking quality. Higher thresholds.
      basis_lookback: 80
      basis_zscore_threshold: 2.2
      oi_divergence_threshold_pct: 2.0
      min_conviction: 0.35
      cooldown_bars: 15

  SPY-PERP:
    parameters:
      # SPY: Tightest basis tracking of all. Highest thresholds.
      basis_lookback: 80
      basis_zscore_threshold: 2.5
      oi_divergence_threshold_pct: 2.0
      min_conviction: 0.35
      cooldown_bars: 15
```

3. **liquidation_cascade.yaml** -- Add per-instrument crypto overrides (QQQ/SPY already disabled via matrix):

```yaml
strategy:
  name: "liquidation_cascade"
  enabled: true
  weight: 0.10

parameters:
  oi_lookback: 10
  oi_drop_threshold_pct: 2.0
  imbalance_threshold: 0.3
  vol_spike_mult: 1.5
  atr_period: 14
  stop_loss_atr_mult: 2.0
  take_profit_atr_mult: 2.5
  min_conviction: 0.45
  cooldown_bars: 12

instruments:
  ETH-PERP:
    parameters:
      # ETH: Regular liquidation events, lower thresholds for more signals (D-04)
      oi_drop_threshold_pct: 1.5
      imbalance_threshold: 0.25
      vol_spike_mult: 1.3
      min_conviction: 0.40
      cooldown_bars: 10

  BTC-PERP:
    parameters:
      # BTC: Deeper liquidity, liquidation events are larger but rarer
      oi_lookback: 12
      oi_drop_threshold_pct: 1.8
      imbalance_threshold: 0.25
      vol_spike_mult: 1.4
      min_conviction: 0.40
      cooldown_bars: 12

  SOL-PERP:
    parameters:
      # SOL: Thin book, frequent small liquidation events
      oi_lookback: 8
      oi_drop_threshold_pct: 1.5
      imbalance_threshold: 0.2
      vol_spike_mult: 1.2
      stop_loss_atr_mult: 2.5
      take_profit_atr_mult: 3.0
      min_conviction: 0.35
      cooldown_bars: 8

  QQQ-PERP:
    enabled: false

  SPY-PERP:
    enabled: false
```

4. **regime_trend.yaml** -- Add ETH-PERP overrides (currently missing per Pitfall 4). Keep existing BTC/SOL/QQQ/SPY overrides but adjust for D-04 (lower thresholds). Update base defaults min_conviction from 0.5 to 0.40.

Update the file to include an `ETH-PERP:` section and lower min_conviction in base defaults:
- Base `min_conviction`: 0.40 (was 0.50)
- Add `ETH-PERP` section:
  ```yaml
  ETH-PERP:
    parameters:
      # ETH: Mid-vol crypto, slightly lower thresholds than defaults (D-04)
      adx_threshold: 20
      atr_expansion_threshold: 1.0
      min_conviction: 0.35
      cooldown_bars: 6
  ```
- Update existing BTC-PERP: add `min_conviction: 0.35`
- Update existing SOL-PERP: add `min_conviction: 0.35`, reduce `cooldown_bars` to 6 (was not set, inherited 8)
- Update existing QQQ-PERP: add `min_conviction: 0.35`
- Update existing SPY-PERP: add `min_conviction: 0.35`

Do NOT modify momentum.yaml (D-09).
  </action>
  <verify>
    <automated>/Users/aroman/Work/phantom-perp/.venv/bin/python -c "
import yaml

for name in ['mean_reversion', 'correlation', 'liquidation_cascade', 'regime_trend']:
    cfg = yaml.safe_load(open(f'configs/strategies/{name}.yaml'))
    instruments = cfg.get('instruments', {})
    print(f'{name}: instruments={sorted(instruments.keys())}')
    assert 'instruments' in cfg, f'{name} missing instruments section'

# Verify ETH-PERP exists in regime_trend
rt = yaml.safe_load(open('configs/strategies/regime_trend.yaml'))
assert 'ETH-PERP' in rt['instruments'], 'regime_trend missing ETH-PERP'

# Verify momentum untouched
mom = yaml.safe_load(open('configs/strategies/momentum.yaml'))
assert 'instruments' not in mom, 'momentum should not have instruments (D-09)'
assert mom['strategy']['enabled'] == False, 'momentum should be disabled'

print('All configs OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - mean_reversion.yaml contains `instruments:` section with ETH-PERP, BTC-PERP, SOL-PERP, QQQ-PERP, SPY-PERP subsections
    - correlation.yaml contains `instruments:` section with all 5 instruments
    - liquidation_cascade.yaml contains `instruments:` section with ETH-PERP, BTC-PERP, SOL-PERP (QQQ/SPY disabled)
    - regime_trend.yaml contains `ETH-PERP:` in instruments section (previously missing per Pitfall 4)
    - regime_trend.yaml base min_conviction is 0.40 or lower (D-04)
    - momentum.yaml is unchanged (no instruments section, enabled: false per D-09)
    - All per-instrument min_conviction values are <= 0.40 (lowered per D-04)
    - QQQ/SPY parameters are completely separate from crypto (D-03), not just slight adjustments
  </acceptance_criteria>
  <done>All 4 active strategy configs have per-instrument parameter overrides for all relevant instruments. Each instrument has research-informed, independent parameters (D-02). Thresholds are lowered to increase signal frequency (D-04). Crypto and equity perps have completely separate parameter sets (D-03). Momentum untouched (D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test per-instrument config loading for all instruments</name>
  <files>agents/signals/tests/test_instrument_configs.py</files>
  <read_first>
    - libs/common/config.py
    - agents/signals/main.py
    - configs/strategies/mean_reversion.yaml
    - configs/strategies/regime_trend.yaml
  </read_first>
  <behavior>
    - test_all_instruments_have_overrides: For each of mean_reversion, correlation, regime_trend: load config for each of 5 instruments, verify the config has an _instrument key and parameters dict
    - test_eth_perp_not_using_bare_defaults: For mean_reversion and regime_trend, load ETH-PERP config, verify at least one parameter differs from the base defaults
    - test_liquidation_cascade_disabled_for_equity: Load liquidation_cascade config for QQQ-PERP and SPY-PERP, verify they are disabled (either via YAML enabled:false or matrix exclusion)
    - test_momentum_disabled_globally: Load momentum config, verify enabled is false
    - test_config_validation_passes_for_all_strategies: For each active strategy, call validate_strategy_config with the raw YAML -- it must not raise ValueError (proving our YAML has no typos)
    - test_per_instrument_min_conviction_lowered: For mean_reversion and regime_trend, verify ETH-PERP min_conviction is <= 0.35 (D-04 signal frequency)
  </behavior>
  <action>
1. Create `agents/signals/tests/test_instrument_configs.py` with the following tests:

- `test_all_instruments_have_overrides`: Use `load_strategy_config_for_instrument()` for each of ["mean_reversion", "correlation", "regime_trend"] x ["ETH-PERP", "BTC-PERP", "SOL-PERP", "QQQ-PERP", "SPY-PERP"]. Assert each returned dict has `_instrument` key matching the instrument and a non-empty `parameters` dict.

- `test_eth_perp_not_using_bare_defaults`: Load `mean_reversion` config for ETH-PERP and compare `parameters` to base config loaded via `load_strategy_config("mean_reversion")["parameters"]`. Assert at least 2 keys differ (per D-02, Pitfall 4: even if defaults are ETH-appropriate, create explicit overrides).

- `test_liquidation_cascade_disabled_for_equity`: Load `liquidation_cascade` config for QQQ-PERP. Verify `config.get("strategy", {}).get("enabled")` is `False`.

- `test_momentum_disabled_globally`: Load `momentum` config. Verify `config["strategy"]["enabled"]` is `False`.

- `test_config_validation_passes_for_all_strategies`: For each strategy in ["mean_reversion", "liquidation_cascade", "correlation", "regime_trend"], load the raw YAML via `load_strategy_config()`, import the corresponding Params class, call `validate_strategy_config()`. Assert no ValueError raised. This proves all our YAML keys are valid.

- `test_per_instrument_min_conviction_lowered`: Load mean_reversion for ETH-PERP, assert `params["min_conviction"] <= 0.35`. Load regime_trend for ETH-PERP, assert `params["min_conviction"] <= 0.40`.

2. Run tests.
  </action>
  <verify>
    <automated>/Users/aroman/Work/phantom-perp/.venv/bin/pytest agents/signals/tests/test_instrument_configs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_instrument_configs.py contains `test_all_instruments_have_overrides`
    - test_instrument_configs.py contains `test_eth_perp_not_using_bare_defaults`
    - test_instrument_configs.py contains `test_liquidation_cascade_disabled_for_equity`
    - test_instrument_configs.py contains `test_momentum_disabled_globally`
    - test_instrument_configs.py contains `test_config_validation_passes_for_all_strategies`
    - test_instrument_configs.py contains `test_per_instrument_min_conviction_lowered`
    - All tests pass
  </acceptance_criteria>
  <done>All 5 instruments (ETH, BTC, SOL, QQQ, SPY) load per-instrument configs correctly. Config validation passes for all strategy YAMLs (no typos). Momentum disabled. Equity perps disabled for liquidation cascade. Thresholds verified as lowered for signal frequency.</done>
</task>

</tasks>

<verification>
Run all tests:
```
/Users/aroman/Work/phantom-perp/.venv/bin/pytest agents/signals/tests/ libs/common/tests/ -x -q
```
All tests pass including config validation, instrument configs, and existing strategy tests.
</verification>

<success_criteria>
1. Strategy matrix exists declaring which strategies run on which instruments
2. All 4 active strategy YAMLs have per-instrument overrides for all relevant instruments
3. ETH-PERP has explicit overrides (not bare defaults) per D-02/Pitfall 4
4. QQQ/SPY have completely separate parameter sets from crypto (D-03)
5. All min_conviction values are lowered per D-04
6. Momentum stays disabled with no per-instrument overrides (D-09)
7. No session logic added (D-12)
8. All config validation passes (no YAML typos)
9. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-tuning/01-03-SUMMARY.md`
</output>
