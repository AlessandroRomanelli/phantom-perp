---
phase: 05-cross-cutting-quality
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - configs/strategies/momentum.yaml
  - configs/strategies/mean_reversion.yaml
  - configs/strategies/correlation.yaml
  - agents/signals/tests/test_instrument_configs.py
autonomous: true
requirements: [XQ-01, XQ-05]

must_haves:
  truths:
    - "Momentum strategy has per-instrument overrides for Phase 2 params (vol_lookback, vol_min_ratio, swing_lookback, swing_order, funding_rate_boost) across all 5 instruments"
    - "Mean reversion strategy has per-instrument overrides for Phase 2 params (trend_reject_threshold, extended_deviation_mult, bb_std_mult adaptivity) across all 5 instruments"
    - "Correlation strategy has per-instrument overrides for Phase 3+4 params (short/medium/long lookback windows, funding params) across all 5 instruments"
    - "All per-instrument values are derived from asset characteristics (D-10): ETH mid-vol, BTC lower-vol longer trends, SOL high-vol, QQQ/SPY equity-session-bound"
  artifacts:
    - path: "configs/strategies/momentum.yaml"
      provides: "Per-instrument momentum overrides including Phase 2+4 params"
      contains: "instruments:"
    - path: "configs/strategies/mean_reversion.yaml"
      provides: "Per-instrument mean reversion overrides including Phase 2+4 params"
      contains: "instruments:"
    - path: "configs/strategies/correlation.yaml"
      provides: "Per-instrument correlation overrides including Phase 3+4 params"
      contains: "instruments:"
  key_links:
    - from: "configs/strategies/momentum.yaml"
      to: "agents/signals/strategies/momentum.py"
      via: "MomentumParams fields"
      pattern: "vol_lookback|vol_min_ratio|swing_lookback|funding_rate_boost"
    - from: "configs/strategies/mean_reversion.yaml"
      to: "agents/signals/strategies/mean_reversion.py"
      via: "MeanReversionParams fields"
      pattern: "trend_reject_threshold|extended_deviation_mult"
---

<objective>
Refresh per-instrument tuning for momentum, mean reversion, and correlation strategies whose logic changed significantly in Phases 2-4. The Phase 1 tuning only covered original params; new params added in Phases 2-4 (volume confirmation, swing stops, funding boost, multi-window basis, adaptive bands) need asset-appropriate values per instrument.

Purpose: Complete per-instrument tuning for strategies with new parameters (D-09, D-10, D-11), ensuring each instrument trades with values derived from its unique characteristics.
Output: Updated YAML configs for 3 strategies with per-instrument overrides for new params.
</objective>

<execution_context>
@/Users/aroman/Work/phantom-perp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aroman/Work/phantom-perp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-cutting-quality/05-01-SUMMARY.md

@configs/strategies/momentum.yaml
@configs/strategies/mean_reversion.yaml
@configs/strategies/correlation.yaml
@configs/strategies/regime_trend.yaml
@agents/signals/strategies/momentum.py
@agents/signals/strategies/mean_reversion.py
@agents/signals/strategies/correlation.py

<interfaces>
<!-- MomentumParams fields needing per-instrument tuning (added in Phase 2+4) -->
From agents/signals/strategies/momentum.py:
```python
@dataclass
class MomentumParams:
    # ... existing Phase 1 params ...
    vol_lookback: int = 10          # Phase 2 (MOM-01)
    vol_min_ratio: float = 0.5     # Phase 2 (MOM-01)
    portfolio_a_min_conviction: float = 0.75  # Phase 2 (MOM-04)
    swing_lookback: int = 20       # Phase 2 (MOM-03)
    swing_order: int = 3           # Phase 2 (MOM-03)
    funding_rate_boost: float = 0.08  # Phase 4
    funding_z_score_threshold: float = 1.5  # Phase 4
    funding_min_samples: int = 10  # Phase 4
```

<!-- MeanReversionParams fields needing tuning (read the file to see all) -->
<!-- CorrelationParams fields needing tuning (read the file to see all) -->

<!-- Reference: regime_trend.yaml shows the pattern for extensive per-instrument overrides -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-instrument momentum tuning for Phase 2+4 params</name>
  <files>configs/strategies/momentum.yaml</files>
  <read_first>
    configs/strategies/momentum.yaml,
    agents/signals/strategies/momentum.py
  </read_first>
  <action>
    Add per-instrument overrides for params added in Phases 2 and 4 to `configs/strategies/momentum.yaml`. Follow D-10 tuning philosophy: derive from asset characteristics, independent per instrument, lower thresholds for activity.

    Add an `instruments:` section (momentum.yaml currently has none) with overrides for each instrument:

    **ETH-PERP** (mid-vol crypto, moderate liquidity):
    ```yaml
    ETH-PERP:
      parameters:
        min_conviction: 0.35
        cooldown_bars: 4
        vol_lookback: 10
        vol_min_ratio: 0.4        # Slightly lower volume gate for ETH
        swing_lookback: 20
        swing_order: 3
        funding_rate_boost: 0.08
        adx_threshold: 18.0       # Lower ADX for mid-vol asset
    ```

    **BTC-PERP** (lower vol, deepest liquidity, longer trends):
    ```yaml
    BTC-PERP:
      parameters:
        min_conviction: 0.35
        cooldown_bars: 8          # Longer cooldown, fewer signals but higher quality
        vol_lookback: 15          # Longer volume lookback for steadier trends
        vol_min_ratio: 0.6        # Higher volume confirmation for BTC
        swing_lookback: 30        # Wider swing search for longer trends
        swing_order: 4            # Require more bars to confirm swing
        funding_rate_boost: 0.06  # Lower boost, BTC funding less volatile
        adx_threshold: 22.0       # Higher ADX threshold for genuine BTC trends
    ```

    **SOL-PERP** (highest vol, thinner book, fastest moves):
    ```yaml
    SOL-PERP:
      parameters:
        min_conviction: 0.30      # Lowest conviction, accept more SOL signals
        cooldown_bars: 3          # Fast retry in volatile SOL
        vol_lookback: 8           # Shorter volume window for fast SOL moves
        vol_min_ratio: 0.35       # Lower volume gate, SOL book is thinner
        swing_lookback: 15        # Shorter swing search, SOL moves fast
        swing_order: 2            # Fewer bars needed for SOL swing
        stop_loss_atr_mult: 2.5   # Wider stops for SOL volatility
        take_profit_atr_mult: 4.0 # Wider targets too
        funding_rate_boost: 0.10  # Higher boost, SOL funding more extreme
        adx_threshold: 18.0       # Lower threshold, SOL trends are noisy
    ```

    **QQQ-PERP** (equity derivative, session-bound):
    ```yaml
    QQQ-PERP:
      parameters:
        min_conviction: 0.35
        cooldown_bars: 10         # Longer cooldown for equity
        vol_lookback: 12
        vol_min_ratio: 0.5
        swing_lookback: 25
        swing_order: 4
        funding_rate_boost: 0.05  # Conservative funding boost
        adx_threshold: 20.0
    ```

    **SPY-PERP** (most liquid equity, steady trends):
    ```yaml
    SPY-PERP:
      parameters:
        min_conviction: 0.35
        cooldown_bars: 12         # Most conservative cooldown
        vol_lookback: 15
        vol_min_ratio: 0.55
        swing_lookback: 25
        swing_order: 4
        funding_rate_boost: 0.04  # Most conservative boost
        adx_threshold: 22.0
    ```
  </action>
  <verify>
    <automated>cd /Users/aroman/Work/phantom-perp && python -c "import yaml; d=yaml.safe_load(open('configs/strategies/momentum.yaml')); insts=d.get('instruments',{}); assert len(insts)==5, f'Expected 5 instruments, got {len(insts)}'; assert all(k in insts for k in ['ETH-PERP','BTC-PERP','SOL-PERP','QQQ-PERP','SPY-PERP']); print('OK: all 5 instruments configured')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "instruments:" configs/strategies/momentum.yaml` matches
    - `grep "ETH-PERP:" configs/strategies/momentum.yaml` matches
    - `grep "BTC-PERP:" configs/strategies/momentum.yaml` matches
    - `grep "SOL-PERP:" configs/strategies/momentum.yaml` matches
    - `grep "QQQ-PERP:" configs/strategies/momentum.yaml` matches
    - `grep "SPY-PERP:" configs/strategies/momentum.yaml` matches
    - `grep "vol_min_ratio:" configs/strategies/momentum.yaml` matches (in instrument sections)
    - `grep "swing_lookback:" configs/strategies/momentum.yaml` matches (in instrument sections)
    - `grep "funding_rate_boost:" configs/strategies/momentum.yaml` matches (in instrument sections)
  </acceptance_criteria>
  <done>Momentum YAML has per-instrument overrides for all 5 instruments covering Phase 2+4 params with asset-appropriate values</done>
</task>

<task type="auto">
  <name>Task 2: Per-instrument tuning for mean reversion and correlation</name>
  <files>
    configs/strategies/mean_reversion.yaml,
    configs/strategies/correlation.yaml
  </files>
  <read_first>
    configs/strategies/mean_reversion.yaml,
    configs/strategies/correlation.yaml,
    agents/signals/strategies/mean_reversion.py,
    agents/signals/strategies/correlation.py
  </read_first>
  <action>
    1. **mean_reversion.yaml** — Add/update per-instrument overrides for Phase 2+4 params. Read the file first to see current state. Add overrides for new params that were introduced in Phase 2 (multi-factor trend rejection, adaptive bands, extended targets, funding boost):

    Per D-10 tuning philosophy for mean reversion:

    **ETH-PERP**: `trend_reject_threshold: 0.6` (more permissive, ETH reverts well), `extended_deviation_mult: 3.2`, `min_conviction: 0.30`, `cooldown_bars: 8`, `funding_rate_boost: 0.08`

    **BTC-PERP**: `trend_reject_threshold: 0.7` (stricter, BTC trends persist), `extended_deviation_mult: 3.5` (wider for BTC), `min_conviction: 0.30`, `cooldown_bars: 12`, `funding_rate_boost: 0.06`, `bb_std_mult: 2.2` (wider bands)

    **SOL-PERP**: `trend_reject_threshold: 0.55` (most permissive, SOL reverts fast), `extended_deviation_mult: 3.0`, `min_conviction: 0.25`, `cooldown_bars: 5`, `bb_std_mult: 2.5` (widest bands for volatility), `stop_loss_atr_mult: 2.5`, `funding_rate_boost: 0.10`

    **QQQ-PERP**: `trend_reject_threshold: 0.65`, `min_conviction: 0.30`, `cooldown_bars: 12`, `funding_rate_boost: 0.05`

    **SPY-PERP**: `trend_reject_threshold: 0.65`, `min_conviction: 0.30`, `cooldown_bars: 15`, `funding_rate_boost: 0.04`

    2. **correlation.yaml** — Add/update per-instrument overrides for Phase 3+4 params (multi-window lookbacks, funding integration):

    **ETH-PERP**: `short_lookback: 25`, `medium_lookback: 50`, `long_lookback: 100`, `min_conviction: 0.30`, `cooldown_bars: 6`, `funding_z_score_threshold: 1.3`

    **BTC-PERP**: `short_lookback: 35`, `medium_lookback: 70`, `long_lookback: 140` (longer windows for BTC), `min_conviction: 0.30`, `cooldown_bars: 10`, `funding_z_score_threshold: 1.5`

    **SOL-PERP**: `short_lookback: 20`, `medium_lookback: 40`, `long_lookback: 80` (shorter for fast SOL), `min_conviction: 0.25`, `cooldown_bars: 4`, `funding_z_score_threshold: 1.2`

    **QQQ-PERP**: `short_lookback: 30`, `medium_lookback: 60`, `long_lookback: 120`, `min_conviction: 0.30`, `cooldown_bars: 12`

    **SPY-PERP**: `short_lookback: 30`, `medium_lookback: 60`, `long_lookback: 120`, `min_conviction: 0.30`, `cooldown_bars: 15`

    Adjust values as needed based on reading the actual params dataclass fields in the strategy files. The above values are directional guidance; the exact field names must match the strategy's params dataclass.
  </action>
  <verify>
    <automated>cd /Users/aroman/Work/phantom-perp && python -c "
import yaml
for f in ['mean_reversion', 'correlation']:
    d = yaml.safe_load(open(f'configs/strategies/{f}.yaml'))
    insts = d.get('instruments', {})
    assert len(insts) >= 5, f'{f}: expected 5 instruments, got {len(insts)}'
    for i in ['ETH-PERP','BTC-PERP','SOL-PERP','QQQ-PERP','SPY-PERP']:
        assert i in insts, f'{f}: missing {i}'
    print(f'OK: {f} has all 5 instruments')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "instruments:" configs/strategies/mean_reversion.yaml` matches
    - `grep "ETH-PERP:" configs/strategies/mean_reversion.yaml` matches
    - `grep "SOL-PERP:" configs/strategies/mean_reversion.yaml` matches
    - `grep "trend_reject_threshold:" configs/strategies/mean_reversion.yaml` matches (in instrument sections)
    - `grep "instruments:" configs/strategies/correlation.yaml` matches
    - `grep "ETH-PERP:" configs/strategies/correlation.yaml` matches
    - `grep "SOL-PERP:" configs/strategies/correlation.yaml` matches
    - Both YAML files parse without errors
    - All 5 instruments present in both files
  </acceptance_criteria>
  <done>Mean reversion and correlation YAMLs have per-instrument overrides for all 5 instruments covering Phase 2-4 params with asset-appropriate values derived from known characteristics</done>
</task>

</tasks>

<verification>
All config files parse correctly and contain per-instrument overrides for all 5 instruments:
```bash
python -c "import yaml; [yaml.safe_load(open(f'configs/strategies/{s}.yaml')) for s in ['momentum','mean_reversion','correlation']]"
python -m pytest agents/signals/tests/ -x -q
```
</verification>

<success_criteria>
- momentum.yaml has per-instrument overrides for Phase 2+4 params across 5 instruments (D-09)
- mean_reversion.yaml has per-instrument overrides for Phase 2+4 params across 5 instruments (D-11)
- correlation.yaml has per-instrument overrides for Phase 3+4 params across 5 instruments (D-11)
- All values reflect asset characteristics: BTC longest, SOL widest/lowest, ETH moderate, QQQ/SPY conservative (D-10)
- All YAML files parse without errors
- Existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-cutting-quality/05-03-SUMMARY.md`
</output>
