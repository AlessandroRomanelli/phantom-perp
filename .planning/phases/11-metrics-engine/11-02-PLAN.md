---
phase: 11-metrics-engine
plan: 02
type: tdd
wave: 2
depends_on: ["11-01"]
files_modified:
  - libs/metrics/engine.py
  - tests/unit/test_metrics_engine.py
autonomous: true
requirements: [METR-01, METR-02, METR-03, METR-04]
must_haves:
  truths:
    - "Expectancy is computed as avg_win * win_rate - avg_loss * loss_rate per (strategy, instrument) from net P&L"
    - "Profit factor is computed as gross_profit / gross_loss per (strategy, instrument); None when no losing trades"
    - "Max drawdown amount is the largest peak-to-trough decline on the cumulative net P&L curve per strategy"
    - "Drawdown duration is measured in calendar hours; uses current time if still in drawdown"
    - "Both gross_pnl and net_pnl are reported in StrategyMetrics output (per D-09)"
    - "Strategy/instrument pairs below 10 trades return None (per D-01, D-02)"
    - "compute_strategy_metrics returns dict[tuple[str, str], StrategyMetrics | None]"
    - "METR-04 is satisfied for trading fees; funding_costs_usdc field is Decimal(0) placeholder per D-08"
    - "Zero-P&L round-trips are classified as losses (conservative, per net_pnl <= 0 convention)"
    - "Zero-drawdown strategies return max_drawdown_usdc=Decimal(0), max_drawdown_duration_hours=0.0"
  artifacts:
    - path: "libs/metrics/engine.py"
      provides: "StrategyMetrics dataclass and compute_strategy_metrics function"
      exports: ["StrategyMetrics", "compute_strategy_metrics"]
      contains: "class StrategyMetrics"
    - path: "tests/unit/test_metrics_engine.py"
      provides: "Unit tests for all METR requirements plus edge cases"
      min_lines: 300
  key_links:
    - from: "libs/metrics/engine.py"
      to: "libs/metrics/engine.py"
      via: "compute_strategy_metrics calls build_round_trips internally"
      pattern: "build_round_trips"
    - from: "libs/metrics/__init__.py"
      to: "libs/metrics/engine.py"
      via: "re-exports StrategyMetrics and compute_strategy_metrics"
      pattern: "from libs.metrics.engine import"
---

<objective>
Implement the metrics computation layer: expectancy (METR-01), profit factor (METR-02), drawdown (METR-03), and the top-level compute_strategy_metrics function with minimum-count gate (D-01/D-02) and fee-adjusted P&L reporting (METR-04/D-09).

Purpose: This is the core deliverable of Phase 11 -- the function that transforms raw fills into actionable per-(strategy, instrument) performance metrics that Claude (Phase 13) will consume. Every locked decision (D-01 through D-09) culminates in this plan's output.

Output: `StrategyMetrics` frozen dataclass, `compute_strategy_metrics()` public function in `libs/metrics/engine.py`. 16+ additional unit tests covering all METR requirements and reviewer-identified edge cases.

NOTE on METR-04 / D-08 resolution (HIGH review concern): METR-04 requires "fee-adjusted P&L (includes trading fees and funding costs)." D-08 explicitly defers funding cost attribution to METR-05/06 because funding costs are per-position, not per-strategy, and Phase 10 does not persist position lifecycle data. Resolution: StrategyMetrics includes a `funding_costs_usdc` field set to `Decimal("0")` as a typed placeholder. The field's docstring documents that funding cost attribution is deferred. `total_net_pnl = total_gross_pnl - total_fees_usdc - funding_costs_usdc`. This satisfies METR-04's interface contract (the field exists, the subtraction happens) while correctly reflecting that funding data is not yet available. Phase 13 (Claude integration) sees the zero and knows funding is not factored in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-metrics-engine/11-CONTEXT.md
@.planning/phases/11-metrics-engine/11-RESEARCH.md
@.planning/phases/11-metrics-engine/11-01-SUMMARY.md

@libs/storage/repository.py
@libs/metrics/engine.py

<interfaces>
<!-- From Plan 01 output: the types this plan builds on -->

From libs/metrics/engine.py (created in Plan 01):
```python
@dataclass(frozen=True, slots=True)
class OrderResult:
    order_id: str
    instrument: str
    primary_source: str
    side: str          # "BUY" or "SELL"
    avg_price: Decimal
    total_size: Decimal
    total_fee: Decimal
    filled_at: datetime

@dataclass(frozen=True, slots=True)
class RoundTrip:
    entry_order_id: str
    exit_order_id: str
    instrument: str
    primary_source: str
    side: str           # entry side "BUY" or "SELL"
    entry_price: Decimal
    exit_price: Decimal
    size: Decimal
    gross_pnl: Decimal
    total_fees: Decimal
    net_pnl: Decimal
    opened_at: datetime
    closed_at: datetime

def build_round_trips(fills: list[AttributedFill]) -> dict[tuple[str, str], list[RoundTrip]]: ...
```

From libs/storage/repository.py:
```python
@dataclass(frozen=True, slots=True)
class AttributedFill:
    fill_id: str
    order_id: str
    portfolio_target: str
    instrument: str
    side: str
    size: Decimal
    price: Decimal
    fee_usdc: Decimal
    is_maker: bool
    filled_at: datetime
    trade_id: str
    primary_source: str
    conviction: float
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Write failing tests for expectancy, profit factor, drawdown, min-count gate, edge cases, and compute_strategy_metrics</name>
  <files>tests/unit/test_metrics_engine.py, libs/metrics/engine.py</files>
  <read_first>
    - libs/metrics/engine.py (current state after Plan 01 -- OrderResult, RoundTrip, build_round_trips)
    - tests/unit/test_metrics_engine.py (existing tests from Plan 01 -- append new tests, do NOT overwrite)
    - .planning/phases/11-metrics-engine/11-RESEARCH.md (exact formulas in "Pattern 3: Metrics Formulas" section)
    - .planning/phases/11-metrics-engine/11-CONTEXT.md (D-01, D-02, D-06, D-07, D-08, D-09 decisions)
  </read_first>
  <behavior>
    - test_expectancy_basic: 10 round-trips (6 wins, 4 losses). Wins: net_pnl = [5, 10, 3, 8, 12, 7] (avg=7.5). Losses: net_pnl = [-4, -6, -3, -2] (avg_loss=3.75). win_rate=0.6, loss_rate=0.4. expectancy = 7.5 * 0.6 - 3.75 * 0.4 = 4.5 - 1.5 = 3.0
    - test_expectancy_all_wins: 10 winning round-trips -> expectancy = avg_win * 1.0 (loss_rate=0, avg_loss=0)
    - test_expectancy_all_losses: 10 losing round-trips -> expectancy = -(avg_loss * 1.0) (win_rate=0, avg_win=0)
    - test_min_count_gate_returns_none: 9 round-trips for a pair -> compute_strategy_metrics returns None for that key
    - test_min_count_gate_boundary: exactly 10 round-trips -> compute_strategy_metrics returns StrategyMetrics (not None)
    - test_profit_factor_basic: gross_wins=[10, 20, 15], gross_losses=[-5, -10] -> profit_factor = 45/15 = 3.0
    - test_profit_factor_no_losses: all winning round-trips -> profit_factor is None (per zero-division guard)
    - test_max_drawdown_amount: cumulative net P&L curve: [5, 15, 10, 8, 20] -> peak at 15, trough at 8, max_dd = 7
    - test_drawdown_duration_hours: peak reached at T+0h, trough at T+48h -> duration = 48.0 hours
    - test_drawdown_duration_still_in_drawdown: last cumulative P&L is below peak -> duration uses freezegun-frozen current time
    - test_drawdown_zero_drawdown: all 10 round-trips are wins with increasing cumulative P&L -> max_drawdown_usdc = Decimal("0"), max_drawdown_duration_hours = 0.0. Verifies no crash or incorrect values when strategy has never been in drawdown.
    - test_fee_adjustment: round-trips have entry_fee + exit_fee; verify total_fees_usdc in StrategyMetrics = sum of all round-trip fees
    - test_gross_and_net_pnl_reported: StrategyMetrics has both total_gross_pnl and total_net_pnl fields, and total_net_pnl = total_gross_pnl - total_fees_usdc - funding_costs_usdc
    - test_funding_costs_placeholder: StrategyMetrics.funding_costs_usdc == Decimal("0") (placeholder per D-08; field exists for future METR-05/06)
    - test_multiple_strategy_instrument_pairs: fills from 2 strategies x 2 instruments -> dict with 4 keys, each with correct metrics or None
    - test_compute_strategy_metrics_empty: empty fill list -> empty dict
  </behavior>
  <action>
    Add stub for `StrategyMetrics` frozen dataclass and `compute_strategy_metrics` function signature to `libs/metrics/engine.py`. The StrategyMetrics dataclass fields:

    ```python
    @dataclass(frozen=True, slots=True)
    class StrategyMetrics:
        """Per-(strategy, instrument) performance summary.

        METR-04 note: funding_costs_usdc is Decimal("0") — funding cost attribution
        is deferred per D-08 (requires position lifecycle tracking not in Phase 10).
        total_net_pnl = total_gross_pnl - total_fees_usdc - funding_costs_usdc.
        """
        primary_source: str
        instrument: str
        trade_count: int
        win_count: int
        loss_count: int
        win_rate: float               # 0.0-1.0
        avg_win_usdc: Decimal
        avg_loss_usdc: Decimal
        expectancy_usdc: Decimal      # METR-01
        profit_factor: float | None   # METR-02; None if no losing trades
        total_gross_pnl: Decimal      # METR-04 gross (D-09)
        total_fees_usdc: Decimal      # METR-04 trading fees
        funding_costs_usdc: Decimal   # METR-04 funding costs (Decimal("0") per D-08)
        total_net_pnl: Decimal        # METR-04 net = gross - fees - funding (D-09)
        max_drawdown_usdc: Decimal    # METR-03 amount
        max_drawdown_duration_hours: float  # METR-03 duration (D-07)
    ```

    Function stub: `def compute_strategy_metrics(fills: list[AttributedFill], min_trades: int = 10) -> dict[tuple[str, str], StrategyMetrics | None]:` raises NotImplementedError.

    APPEND the 16 test functions to `tests/unit/test_metrics_engine.py` (do NOT delete existing tests from Plan 01).

    For tests that need RoundTrip objects, create a `_make_round_trip()` factory helper with defaults: entry_order_id auto-generated, exit_order_id auto-generated, instrument="ETH-PERP-INTX", primary_source="momentum", side="BUY", entry_price=Decimal("100"), exit_price=Decimal("110"), size=Decimal("1.0"), gross_pnl=Decimal("10"), total_fees=Decimal("1.0"), net_pnl=Decimal("9.0"), opened_at=datetime(2026,1,1,0,0, tzinfo=timezone.utc), closed_at=datetime(2026,1,1,1,0, tzinfo=timezone.utc). All keyword args overrideable.

    For test_drawdown_duration_still_in_drawdown, use `from freezegun import freeze_time` with `@freeze_time("2026-01-05 00:00:00")`.

    For test_compute_strategy_metrics_empty and test_multiple_strategy_instrument_pairs, use `_make_fill()` factory from Plan 01 and call `compute_strategy_metrics()` directly.

    For test_min_count_gate_returns_none and test_min_count_gate_boundary, create 9 or 10 fills that form complete round-trips (pairs of BUY/SELL fills with different order_ids) and call `compute_strategy_metrics()`.

    For test_funding_costs_placeholder, create 10 round-trips, call compute_strategy_metrics, and assert result.funding_costs_usdc == Decimal("0").

    For test_drawdown_zero_drawdown, create 10 round-trips all with positive net_pnl, verify max_drawdown_usdc == Decimal("0") and max_drawdown_duration_hours == 0.0.

    For test_expectancy_all_losses, create 10 round-trips all with negative net_pnl, verify expectancy is negative.

    Run `python -m pytest tests/unit/test_metrics_engine.py -x -k "test_expectancy or test_min_count or test_profit_factor or test_max_drawdown or test_drawdown or test_fee_adjustment or test_gross_and_net or test_funding or test_multiple_strategy or test_compute_strategy_metrics_empty"` -- new tests must FAIL, old tests must still PASS.

    Commit: `test(11-02): add failing tests for expectancy, profit factor, drawdown, min-count gate, edge cases`
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_metrics_engine.py -x -k "test_expectancy_basic" 2>&1 | tail -3</automated>
    Expect: FAILED (NotImplementedError). Old Plan 01 tests still pass: `python -m pytest tests/unit/test_metrics_engine.py -k "test_vwap or test_pair or test_build" -x` exits 0.
  </verify>
  <acceptance_criteria>
    - tests/unit/test_metrics_engine.py contains all 16 new test function names: test_expectancy_basic, test_expectancy_all_wins, test_expectancy_all_losses, test_min_count_gate_returns_none, test_min_count_gate_boundary, test_profit_factor_basic, test_profit_factor_no_losses, test_max_drawdown_amount, test_drawdown_duration_hours, test_drawdown_duration_still_in_drawdown, test_drawdown_zero_drawdown, test_fee_adjustment, test_gross_and_net_pnl_reported, test_funding_costs_placeholder, test_multiple_strategy_instrument_pairs, test_compute_strategy_metrics_empty
    - tests/unit/test_metrics_engine.py contains `def _make_round_trip(`
    - tests/unit/test_metrics_engine.py contains `from freezegun import freeze_time`
    - tests/unit/test_metrics_engine.py `test_funding_costs_placeholder` asserts `funding_costs_usdc == Decimal("0")`
    - tests/unit/test_metrics_engine.py `test_drawdown_zero_drawdown` asserts `max_drawdown_usdc == Decimal("0")`
    - libs/metrics/engine.py contains `class StrategyMetrics`
    - libs/metrics/engine.py contains `funding_costs_usdc: Decimal`
    - libs/metrics/engine.py contains `def compute_strategy_metrics`
    - `python -m pytest tests/unit/test_metrics_engine.py --co -q` lists 30 test items (14 from Plan 01 + 16 new)
  </acceptance_criteria>
  <done>16 new test functions exist and fail with NotImplementedError. 14 existing tests from Plan 01 still pass. Total: 30 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Implement StrategyMetrics computation, min-count gate, and compute_strategy_metrics</name>
  <files>libs/metrics/engine.py, libs/metrics/__init__.py</files>
  <read_first>
    - libs/metrics/engine.py (current state with stubs)
    - tests/unit/test_metrics_engine.py (all 30 test expectations)
    - .planning/phases/11-metrics-engine/11-RESEARCH.md (exact formulas, anti-patterns, pitfalls)
    - .planning/phases/11-metrics-engine/11-CONTEXT.md (D-01 through D-09)
  </read_first>
  <behavior>
    - compute_strategy_metrics groups fills, builds round-trips, applies min-count gate, computes all metrics
    - Expectancy uses net_pnl for win/loss classification; converts float rates to Decimal for monetary arithmetic
    - Profit factor uses gross_pnl for win/loss sums; returns None when gross_loss == 0
    - Drawdown tracks peak on cumulative net_pnl curve; duration in hours; checks still-in-drawdown at end; returns 0/0.0 when never in drawdown
    - Min-count gate: len(round_trips) < min_trades -> None in result dict
    - funding_costs_usdc always Decimal("0") per D-08; total_net_pnl = total_gross_pnl - total_fees_usdc - funding_costs_usdc
  </behavior>
  <action>
    Implement `compute_strategy_metrics` in `libs/metrics/engine.py`:

    **Internal helper `_compute_metrics(source: str, instrument: str, round_trips: list[RoundTrip]) -> StrategyMetrics`:**

    1. **Win/Loss classification (net_pnl basis per D-09):**
       - wins = [rt for rt in round_trips if rt.net_pnl > Decimal("0")]
       - losses = [rt for rt in round_trips if rt.net_pnl <= Decimal("0")]
       - Zero-P&L trades classified as losses (conservative -- fees make true breakeven negative)
       - win_count = len(wins), loss_count = len(losses), trade_count = len(round_trips)
       - win_rate = win_count / trade_count (float, 0.0-1.0)

    2. **Expectancy (METR-01):**
       - avg_win = sum(rt.net_pnl for rt in wins) / win_count if wins else Decimal("0")
       - avg_loss = sum(abs(rt.net_pnl) for rt in losses) / loss_count if losses else Decimal("0")
       - expectancy = avg_win * Decimal(str(win_rate)) - avg_loss * Decimal(str(1.0 - win_rate))
       - Guard: if no wins, avg_win = Decimal("0"); if no losses, avg_loss = Decimal("0")

    3. **Profit Factor (METR-02):**
       - gross_profit = sum(rt.gross_pnl for rt in round_trips if rt.gross_pnl > Decimal("0"))
       - gross_loss = abs(sum(rt.gross_pnl for rt in round_trips if rt.gross_pnl < Decimal("0")))
       - profit_factor = float(gross_profit / gross_loss) if gross_loss > Decimal("0") else None
       - Breakeven trades (gross_pnl == 0) excluded from both sums

    4. **Drawdown (METR-03):**
       - Sort round_trips by closed_at (defensive sort)
       - cumulative = Decimal("0"), peak = Decimal("0"), peak_time = round_trips[0].closed_at
       - max_dd_amount = Decimal("0"), max_dd_duration_hours = 0.0
       - For each rt: cumulative += rt.net_pnl
         - If cumulative > peak: peak = cumulative, peak_time = rt.closed_at
         - dd = peak - cumulative
         - If dd > max_dd_amount: max_dd_amount = dd, max_dd_duration_hours = (rt.closed_at - peak_time).total_seconds() / 3600
       - After loop: if cumulative < peak (still in drawdown per D-07):
         - still_dd_duration = (datetime.now(timezone.utc) - peak_time).total_seconds() / 3600
         - If still_dd_duration > max_dd_duration_hours: max_dd_duration_hours = still_dd_duration
       - If strategy was never in drawdown (all trades positive, cumulative always at peak): max_dd_amount stays Decimal("0"), max_dd_duration_hours stays 0.0

    5. **Fee-adjusted aggregates (METR-04/D-09) with funding placeholder (D-08):**
       - total_gross_pnl = sum(rt.gross_pnl for rt in round_trips)
       - total_fees_usdc = sum(rt.total_fees for rt in round_trips)
       - funding_costs_usdc = Decimal("0")  # D-08: deferred to METR-05/06
       - total_net_pnl = total_gross_pnl - total_fees_usdc - funding_costs_usdc

    **Public function `compute_strategy_metrics(fills, min_trades=10)`:**
    - Call build_round_trips(fills) to get dict[tuple[str,str], list[RoundTrip]]
    - For each (source, instrument), round_trips in result:
      - If len(round_trips) < min_trades: result[(source, instrument)] = None (per D-01/D-02)
      - Else: result[(source, instrument)] = _compute_metrics(source, instrument, round_trips)
    - Return dict

    Update `libs/metrics/__init__.py` to add StrategyMetrics and compute_strategy_metrics to imports and __all__.

    Run `python -m pytest tests/unit/test_metrics_engine.py -x -v` -- ALL 30 tests must PASS.

    Commit: `feat(11-02): implement metrics computation -- expectancy, profit factor, drawdown, min-count gate`
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_metrics_engine.py -x -v</automated>
    Expect: 30 tests pass, 0 failures.
  </verify>
  <acceptance_criteria>
    - libs/metrics/engine.py does NOT contain `raise NotImplementedError`
    - libs/metrics/engine.py contains `def _compute_metrics(`
    - libs/metrics/engine.py contains `def compute_strategy_metrics(`
    - libs/metrics/engine.py contains `min_trades: int = 10` in compute_strategy_metrics signature
    - libs/metrics/engine.py contains `Decimal(str(win_rate))` or equivalent Decimal conversion for monetary arithmetic
    - libs/metrics/engine.py contains `datetime.now(timezone.utc)` for still-in-drawdown check
    - libs/metrics/engine.py contains `if gross_loss` guard before profit factor division
    - libs/metrics/engine.py contains `funding_costs_usdc = Decimal("0")` or equivalent
    - libs/metrics/engine.py StrategyMetrics docstring contains "D-08" or "funding cost" or "deferred"
    - libs/metrics/__init__.py contains `StrategyMetrics` and `compute_strategy_metrics`
    - `python -m pytest tests/unit/test_metrics_engine.py -x` exits with code 0
    - `python -m pytest tests/unit/test_metrics_engine.py --co -q` lists 30 test items
  </acceptance_criteria>
  <done>All 30 tests pass. Expectancy, profit factor, drawdown (amount + duration + zero-drawdown), fee-adjusted P&L with funding placeholder, and min-count gate are implemented and verified. StrategyMetrics exported from libs/metrics/__init__.py.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/test_metrics_engine.py -x -v` -- all 30 tests pass
- `python -c "from libs.metrics import StrategyMetrics, compute_strategy_metrics; print('imports OK')"` -- no import errors
- `python -c "from libs.metrics.engine import StrategyMetrics; import dataclasses; print([f.name for f in dataclasses.fields(StrategyMetrics)])"` -- confirms all expected fields present including funding_costs_usdc
- `python -m pytest tests/unit/ -x` -- full unit test suite still passes (no regressions)
</verification>

<success_criteria>
- METR-01: Expectancy computed as avg_win * win_rate - avg_loss * loss_rate from net P&L round-trips, verified with hand-calculated test values including all-wins and all-losses edge cases
- METR-02: Profit factor = gross_profit / gross_loss; returns None when gross_loss == 0; verified with known inputs
- METR-03: Max drawdown amount and duration computed from cumulative net P&L curve; still-in-drawdown uses current time; zero-drawdown returns 0/0.0; verified with deterministic test scenarios
- METR-04: Trading fees subtracted via total_fees_usdc. Funding costs deferred per D-08 -- funding_costs_usdc field exists as Decimal("0") placeholder. total_net_pnl = total_gross_pnl - total_fees_usdc - funding_costs_usdc. Both gross and net P&L reported per D-09.
- D-01/D-02: Pairs below 10 trades return None in result dict; boundary at exactly 10 trades returns StrategyMetrics
- D-09: Both total_gross_pnl and total_net_pnl present in StrategyMetrics output
- 30 unit tests pass covering all requirements and edge cases (zero-P&L, zero-drawdown, all-wins, all-losses, funding placeholder)
</success_criteria>

<output>
After completion, create `.planning/phases/11-metrics-engine/11-02-SUMMARY.md`
</output>
