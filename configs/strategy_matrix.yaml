# Strategy Matrix — which strategies run on which instruments.
# This is the single source of truth for per-instrument enablement.
# Individual strategy YAMLs control global enabled/disabled state;
# this matrix controls per-instrument enablement within that.

strategies:
  momentum:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      # - QQQ-PERP  # disabled: Coinbase equity perps misbehaving — revisit later
      # - SPY-PERP  # disabled: Coinbase equity perps misbehaving — revisit later

  mean_reversion:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      # - QQQ-PERP  # disabled: Coinbase equity perps misbehaving — revisit later
      # - SPY-PERP  # disabled: Coinbase equity perps misbehaving — revisit later

  liquidation_cascade:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      # QQQ/SPY disabled: liquidation cascades are crypto-native (D-11)

  correlation:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      # - QQQ-PERP  # disabled: Coinbase equity perps misbehaving — revisit later
      # - SPY-PERP  # disabled: Coinbase equity perps misbehaving — revisit later

  regime_trend:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      # - QQQ-PERP  # disabled: Coinbase equity perps misbehaving — revisit later
      # - SPY-PERP  # disabled: Coinbase equity perps misbehaving — revisit later

  orderbook_imbalance:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      # - QQQ-PERP  # disabled: Coinbase equity perps misbehaving — revisit later
      # - SPY-PERP  # disabled: Coinbase equity perps misbehaving — revisit later

  vwap:
    enabled: true
    instruments:
      - ETH-PERP
      - BTC-PERP
      - SOL-PERP
      # - QQQ-PERP  # disabled: Coinbase equity perps misbehaving — revisit later
      # - SPY-PERP  # disabled: Coinbase equity perps misbehaving — revisit later
